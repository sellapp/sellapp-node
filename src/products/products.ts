// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { ProductsListOptions } from './interfaces/products-list-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { BatchCreateOptions } from './interfaces/batch-create-options.interface';
import type { BatchUpdateOptions } from './interfaces/batch-update-options.interface';
import type { BatchDeleteOptions } from './interfaces/batch-delete-options.interface';
import type { ListProductsResponseValue200ApplicationJsonPropertyDataItem, ListProductsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-products-response-value-200-application-json-property-data-item.interface';
import type { SdkCreateProductResponseValue201ApplicationJson, SdkCreateProductResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-product-response-value-201-application-json.interface';
import type { SdkGetProductResponseValue200ApplicationJson, SdkGetProductResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-product-response-value-200-application-json.interface';
import type { SdkReplaceProductResponseValue200ApplicationJson, SdkReplaceProductResponseValue200ApplicationJsonResponse } from './interfaces/sdk-replace-product-response-value-200-application-json.interface';
import type { SdkUpdateProductResponseValue200ApplicationJson, SdkUpdateProductResponseValue200ApplicationJsonResponse } from './interfaces/sdk-update-product-response-value-200-application-json.interface';
import type { SearchProductsResponseValue200ApplicationJsonPropertyDataItem, SearchProductsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/search-products-response-value-200-application-json-property-data-item.interface';
import type { SdkBatchCreateProductsResponseValue200ApplicationJson, SdkBatchCreateProductsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-batch-create-products-response-value-200-application-json.interface';
import type { SdkBatchUpdateProductsResponseValue200ApplicationJson, SdkBatchUpdateProductsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-batch-update-products-response-value-200-application-json.interface';
import type { SdkCreateProductRequestApplicationJsonResponse } from './interfaces/sdk-create-product-request-application-json.interface';
import type { SdkReplaceProductRequestApplicationJsonResponse } from './interfaces/sdk-replace-product-request-application-json.interface';
import type { SdkUpdateProductRequestApplicationJsonResponse } from './interfaces/sdk-update-product-request-application-json.interface';
import type { SdkDeleteProductRequestApplicationJsonResponse } from './interfaces/sdk-delete-product-request-application-json.interface';
import type { SdkSearchProductsRequestApplicationJsonResponse } from './interfaces/sdk-search-products-request-application-json.interface';
import type { SdkBatchCreateProductsRequestApplicationJsonResponse } from './interfaces/sdk-batch-create-products-request-application-json.interface';
import type { SdkBatchUpdateProductsRequestApplicationJsonResponse } from './interfaces/sdk-batch-update-products-request-application-json.interface';
import type { SdkBatchDeleteProductsRequestApplicationJsonResponse } from './interfaces/sdk-batch-delete-products-request-application-json.interface';
import { deserializeListProductsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-products-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkCreateProductResponseValue201ApplicationJson } from './serializers/sdk-create-product-response-value-201-application-json.serializer';
import { deserializeSdkGetProductResponseValue200ApplicationJson } from './serializers/sdk-get-product-response-value-200-application-json.serializer';
import { deserializeSdkReplaceProductResponseValue200ApplicationJson } from './serializers/sdk-replace-product-response-value-200-application-json.serializer';
import { deserializeSdkUpdateProductResponseValue200ApplicationJson } from './serializers/sdk-update-product-response-value-200-application-json.serializer';
import { deserializeSearchProductsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/search-products-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkBatchCreateProductsResponseValue200ApplicationJson } from './serializers/sdk-batch-create-products-response-value-200-application-json.serializer';
import { deserializeSdkBatchUpdateProductsResponseValue200ApplicationJson } from './serializers/sdk-batch-update-products-response-value-200-application-json.serializer';
import { serializeSdkCreateProductRequestApplicationJson } from './serializers/sdk-create-product-request-application-json.serializer';
import { serializeSdkReplaceProductRequestApplicationJson } from './serializers/sdk-replace-product-request-application-json.serializer';
import { serializeSdkUpdateProductRequestApplicationJson } from './serializers/sdk-update-product-request-application-json.serializer';
import { serializeSdkDeleteProductRequestApplicationJson } from './serializers/sdk-delete-product-request-application-json.serializer';
import { serializeSdkSearchProductsRequestApplicationJson } from './serializers/sdk-search-products-request-application-json.serializer';
import { serializeSdkBatchCreateProductsRequestApplicationJson } from './serializers/sdk-batch-create-products-request-application-json.serializer';
import { serializeSdkBatchUpdateProductsRequestApplicationJson } from './serializers/sdk-batch-update-products-request-application-json.serializer';
import { serializeSdkBatchDeleteProductsRequestApplicationJson } from './serializers/sdk-batch-delete-products-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

const serializeProductsListOptions = (options: ProductsListOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.page !== undefined) wire.page = options.page;
  if (options.withDrafts !== undefined) wire.with_drafts = options.withDrafts;
  if (options.onlyDrafts !== undefined) wire.only_drafts = options.onlyDrafts;
  return wire as PaginationOptions;
};

export class Products {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List all products
   *
   * List your store's products, 15 per page by default. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListProductsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: ProductsListOptions): Promise<AutoPaginatable<ListProductsResponseValue200ApplicationJsonPropertyDataItem, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<ListProductsResponseValue200ApplicationJsonPropertyDataItemResponse, ListProductsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/products',
        deserializeListProductsResponseValue200ApplicationJsonPropertyDataItem,
        options ? serializeProductsListOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<ListProductsResponseValue200ApplicationJsonPropertyDataItemResponse, ListProductsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/products',
          deserializeListProductsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      options ? serializeProductsListOptions(options) : undefined,
    );
  }

  /**
   * Create a product
   *
   * Create a product in the selected store. Requires a token with the `listing` ability. The same product rules apply as in the dashboard. Add a variant before offering the product at checkout. Include variants to create the product and its initial variants atomically. For a complete bundle, set type to bundle, supply one BUNDLE variant with empty deliverable.data, and include bundle_items. The response includes the created variant IDs in data.variants. Omit these fields to retain separate creation. These options apply to single-product creation, not batch creation. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkCreateProductResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateProductResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateProductResponseValue201ApplicationJsonResponse, SdkCreateProductRequestApplicationJsonResponse>(
      '/v2/products',
      serializeSdkCreateProductRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createProduct' },
    );
    return deserializeSdkCreateProductResponseValue201ApplicationJson(data);
  }

  /**
   * Retrieve a product
   *
   * Retrieve a product by its ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.product - The product path parameter.
   * @example 1
   * @param options.withDrafts - Include draft products alongside published products.
   * @param options.onlyDrafts - Return only draft products.
   * @returns {Promise<SdkGetProductResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetProductResponseValue200ApplicationJson> {
    const { product } = options;
    const { data } = await this.sellApp.get<SdkGetProductResponseValue200ApplicationJsonResponse>(`/v2/products/${encodeURIComponent(product)}`, { ...options.requestOptions, operationId: 'getProduct', query: { ...(options.withDrafts !== undefined && { with_drafts: options.withDrafts }), ...(options.onlyDrafts !== undefined && { only_drafts: options.onlyDrafts }) } });
    return deserializeSdkGetProductResponseValue200ApplicationJson(data);
  }

  /**
   * Update a product
   *
   * Update a product in the authenticated store. Requires a token with the `listing` ability. Send `expected_updated_at` to reject stale writes instead of overwriting a newer change. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @example 120
   * @returns {Promise<SdkReplaceProductResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceProductResponseValue200ApplicationJson> {
    const { product, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceProductResponseValue200ApplicationJsonResponse, SdkReplaceProductRequestApplicationJsonResponse>(
      `/v2/products/${encodeURIComponent(product)}`,
      serializeSdkReplaceProductRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceProduct' },
    );
    return deserializeSdkReplaceProductResponseValue200ApplicationJson(data);
  }

  /**
   * Update a product
   *
   * Update a product in the authenticated store. Requires a token with the `listing` ability. Send `expected_updated_at` to reject stale writes instead of overwriting a newer change. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @example 120
   * @returns {Promise<SdkUpdateProductResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateProductResponseValue200ApplicationJson> {
    const { product, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateProductResponseValue200ApplicationJsonResponse, SdkUpdateProductRequestApplicationJsonResponse>(
      `/v2/products/${encodeURIComponent(product)}`,
      serializeSdkUpdateProductRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateProduct' },
    );
    return deserializeSdkUpdateProductResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a product
   *
   * Soft-delete a product in the authenticated store. Products with active or past-due subscriptions cannot be deleted. Send `expected_updated_at` to reject a stale deletion. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @example 1
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { product, requestOptions, ...payload } = options;
    await this.sellApp.deleteWithBody<SdkDeleteProductRequestApplicationJsonResponse>(`/v2/products/${encodeURIComponent(product)}`, serializeSdkDeleteProductRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'deleteProduct' });
  }

  /**
   * Search products
   *
   * Search products using JSON body filters, search terms, includes, and sort instructions. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SearchProductsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchProductsResponseValue200ApplicationJson> {
    const { page, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchProductsResponseValue200ApplicationJsonResponse, SdkSearchProductsRequestApplicationJsonResponse>(
      '/v2/products/search',
      serializeSdkSearchProductsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchProducts', query: { ...(options.page !== undefined && { page: options.page }) } },
    );
    return deserializeSdkSearchProductsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch create products
   *
   * Create multiple products in one request by sending a `resources` array. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkBatchCreateProductsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async batchCreate(options: BatchCreateOptions): Promise<SdkBatchCreateProductsResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkBatchCreateProductsResponseValue200ApplicationJsonResponse, SdkBatchCreateProductsRequestApplicationJsonResponse>(
      '/v2/products/batch',
      serializeSdkBatchCreateProductsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'batchCreateProducts' },
    );
    return deserializeSdkBatchCreateProductsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch update products
   *
   * Update multiple products in one request by sending a `resources` object keyed by product ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkBatchUpdateProductsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async batchUpdate(options: BatchUpdateOptions): Promise<SdkBatchUpdateProductsResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkBatchUpdateProductsResponseValue200ApplicationJsonResponse, SdkBatchUpdateProductsRequestApplicationJsonResponse>(
      '/v2/products/batch',
      serializeSdkBatchUpdateProductsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'batchUpdateProducts' },
    );
    return deserializeSdkBatchUpdateProductsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch delete products
   *
   * Delete multiple products in one request by sending the product IDs in `resources`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async batchDelete(options: BatchDeleteOptions): Promise<void> {
    const { requestOptions, ...payload } = options;
    await this.sellApp.deleteWithBody<SdkBatchDeleteProductsRequestApplicationJsonResponse>('/v2/products/batch', serializeSdkBatchDeleteProductsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'batchDeleteProducts' });
  }
}
