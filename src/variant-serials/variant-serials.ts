// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { VariantSerialsListOptions } from './interfaces/variant-serials-list-options.interface';
import type { AppendOptions } from './interfaces/append-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { QueueOptions } from './interfaces/queue-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem, ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-variant-serial-inventory-response-value-200-application-json-property-data-item.interface';
import type { SdkAppendVariantSerialInventoryResponseValue201ApplicationJson, SdkAppendVariantSerialInventoryResponseValue201ApplicationJsonResponse } from './interfaces/sdk-append-variant-serial-inventory-response-value-201-application-json.interface';
import type { SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson, SdkReplaceVariantSerialInventoryResponseValue200ApplicationJsonResponse } from './interfaces/sdk-replace-variant-serial-inventory-response-value-200-application-json.interface';
import type { SdkQueueVariantSerialImportResponseValue202ApplicationJson, SdkQueueVariantSerialImportResponseValue202ApplicationJsonResponse } from './interfaces/sdk-queue-variant-serial-import-response-value-202-application-json.interface';
import type { SdkAppendVariantSerialInventoryRequestApplicationJsonResponse } from './interfaces/sdk-append-variant-serial-inventory-request-application-json.interface';
import type { SdkReplaceVariantSerialInventoryRequestApplicationJsonResponse } from './interfaces/sdk-replace-variant-serial-inventory-request-application-json.interface';
import type { SdkQueueVariantSerialImportRequestMultipartFormDataResponse } from './interfaces/sdk-queue-variant-serial-import-request-multipart-form-data.interface';
import { deserializeListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-variant-serial-inventory-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkAppendVariantSerialInventoryResponseValue201ApplicationJson } from './serializers/sdk-append-variant-serial-inventory-response-value-201-application-json.serializer';
import { deserializeSdkReplaceVariantSerialInventoryResponseValue200ApplicationJson } from './serializers/sdk-replace-variant-serial-inventory-response-value-200-application-json.serializer';
import { deserializeSdkQueueVariantSerialImportResponseValue202ApplicationJson } from './serializers/sdk-queue-variant-serial-import-response-value-202-application-json.serializer';
import { serializeSdkAppendVariantSerialInventoryRequestApplicationJson } from './serializers/sdk-append-variant-serial-inventory-request-application-json.serializer';
import { serializeSdkReplaceVariantSerialInventoryRequestApplicationJson } from './serializers/sdk-replace-variant-serial-inventory-request-application-json.serializer';
import { serializeSdkQueueVariantSerialImportRequestMultipartFormData } from './serializers/sdk-queue-variant-serial-import-request-multipart-form-data.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class VariantSerials {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List variant serial inventory
   *
   * Lists unsold serial inventory for a variant in the current store. Requires the `listing` Sanctum ability. Serial metadata is not exposed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @param options.product - The product path parameter.
   * @param options.variant - The variant path parameter.
   * @returns {Promise<AutoPaginatable<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options: VariantSerialsListOptions): Promise<AutoPaginatable<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem, Omit<VariantSerialsListOptions, 'product' | 'variant'>>> {
    const { product, variant, ...paginationOptions } = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItemResponse, ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        `/v2/products/${encodeURIComponent(product)}/variants/${encodeURIComponent(variant)}/serials`,
        deserializeListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItemResponse, ListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          `/v2/products/${encodeURIComponent(product)}/variants/${encodeURIComponent(variant)}/serials`,
          deserializeListVariantSerialInventoryResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Append variant serial inventory
   *
   * Atomically appends unsold serials and recalculates variant stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @param options.variant - The variant path parameter.
   * @returns {Promise<SdkAppendVariantSerialInventoryResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async append(options: AppendOptions): Promise<SdkAppendVariantSerialInventoryResponseValue201ApplicationJson> {
    const { product, variant, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkAppendVariantSerialInventoryResponseValue201ApplicationJsonResponse, SdkAppendVariantSerialInventoryRequestApplicationJsonResponse>(
      `/v2/products/${encodeURIComponent(product)}/variants/${encodeURIComponent(variant)}/serials`,
      serializeSdkAppendVariantSerialInventoryRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'appendVariantSerialInventory' },
    );
    return deserializeSdkAppendVariantSerialInventoryResponseValue201ApplicationJson(data);
  }

  /**
   * Replace variant serial inventory
   *
   * Atomically replaces all unsold serials and recalculates variant stock. Concurrent inventory mutations are serialized. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @param options.variant - The variant path parameter.
   * @returns {Promise<SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceVariantSerialInventoryResponseValue200ApplicationJson> {
    const { product, variant, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceVariantSerialInventoryResponseValue200ApplicationJsonResponse, SdkReplaceVariantSerialInventoryRequestApplicationJsonResponse>(
      `/v2/products/${encodeURIComponent(product)}/variants/${encodeURIComponent(variant)}/serials`,
      serializeSdkReplaceVariantSerialInventoryRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceVariantSerialInventory' },
    );
    return deserializeSdkReplaceVariantSerialInventoryResponseValue200ApplicationJson(data);
  }

  /**
   * Queue a variant serial import
   *
   * Uploads a text file to application-owned temporary storage and queues an atomic serial import. Use this endpoint for large imports. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @param options.variant - The variant path parameter.
   * @returns {Promise<SdkQueueVariantSerialImportResponseValue202ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async queue(options: QueueOptions): Promise<SdkQueueVariantSerialImportResponseValue202ApplicationJson> {
    const { product, variant, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkQueueVariantSerialImportResponseValue202ApplicationJsonResponse, SdkQueueVariantSerialImportRequestMultipartFormDataResponse>(
      `/v2/products/${encodeURIComponent(product)}/variants/${encodeURIComponent(variant)}/serials/import`,
      serializeSdkQueueVariantSerialImportRequestMultipartFormData(payload), { ...options.requestOptions, operationId: 'queueVariantSerialImport' },
    );
    return deserializeSdkQueueVariantSerialImportResponseValue202ApplicationJson(data);
  }

  /**
   * Delete a variant serial
   *
   * Deletes one unsold serial scoped to the current store, product, and variant, then recalculates stock. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.product - The product path parameter.
   * @param options.variant - The variant path parameter.
   * @param options.serial - The unsold serial entry ULID.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { product, variant, serial } = options;
    await this.sellApp.delete(`/v2/products/${encodeURIComponent(product)}/variants/${encodeURIComponent(variant)}/serials/${encodeURIComponent(serial)}`, { ...options.requestOptions, operationId: 'deleteVariantSerial' });
  }
}
