// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { CreditsBalancesListOptions } from './interfaces/credits-balances-list-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { SdkListCreditBalancesResponseValue200ApplicationJson, SdkListCreditBalancesResponseValue200ApplicationJsonResponse } from '../credits-products/interfaces/sdk-list-credit-balances-response-value-200-application-json.interface';
import type { SdkGetCreditBalanceResponseValue200ApplicationJson, SdkGetCreditBalanceResponseValue200ApplicationJsonResponse } from '../credits-products/interfaces/sdk-get-credit-balance-response-value-200-application-json.interface';
import { deserializeSdkListCreditBalancesResponseValue200ApplicationJson } from '../credits-products/serializers/sdk-list-credit-balances-response-value-200-application-json.serializer';
import { deserializeSdkGetCreditBalanceResponseValue200ApplicationJson } from '../credits-products/serializers/sdk-get-credit-balance-response-value-200-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

const serializeCreditsBalancesListOptions = (options: CreditsBalancesListOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.page !== undefined) wire.page = options.page;
  if (options.pagination !== undefined) wire.pagination = options.pagination;
  if (options.customerId !== undefined) wire.customer_id = options.customerId;
  if (options.productId !== undefined) wire.product_id = options.productId;
  return wire as PaginationOptions;
};

export class CreditsBalances {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List credit balances
   *
   * List store-scoped customer balances for dedicated credits products. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SdkListCreditBalancesResponseValue200ApplicationJson>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: CreditsBalancesListOptions): Promise<AutoPaginatable<SdkListCreditBalancesResponseValue200ApplicationJson, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<SdkListCreditBalancesResponseValue200ApplicationJsonResponse, SdkListCreditBalancesResponseValue200ApplicationJson>(
        this.sellApp,
        '/v2/credit-balances',
        deserializeSdkListCreditBalancesResponseValue200ApplicationJson,
        options ? serializeCreditsBalancesListOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<SdkListCreditBalancesResponseValue200ApplicationJsonResponse, SdkListCreditBalancesResponseValue200ApplicationJson>(
          this.sellApp,
          '/v2/credit-balances',
          deserializeSdkListCreditBalancesResponseValue200ApplicationJson,
          params,
        ),
      options ? serializeCreditsBalancesListOptions(options) : undefined,
    );
  }

  /**
   * Retrieve a credit balance
   *
   * Retrieve one customer and credits-product balance with up to 100 recent ledger entries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customer - The customer path parameter.
   * @param options.creditProduct - The creditProduct path parameter.
   * @returns {Promise<SdkGetCreditBalanceResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetCreditBalanceResponseValue200ApplicationJson> {
    const { customer, creditProduct } = options;
    const { data } = await this.sellApp.get<SdkGetCreditBalanceResponseValue200ApplicationJsonResponse>(`/v2/credit-balances/${encodeURIComponent(customer)}/${encodeURIComponent(creditProduct)}`, { ...options.requestOptions, operationId: 'getCreditBalance' });
    return deserializeSdkGetCreditBalanceResponseValue200ApplicationJson(data);
  }
}
