// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { WebhookChannelsListOptions } from './interfaces/webhook-channels-list-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { RotateOptions } from './interfaces/rotate-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { SendOptions } from './interfaces/send-options.interface';
import type { ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem, ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-webhook-channels-response-value-200-application-json-property-data-item.interface';
import type { SdkCreateWebhookChannelResponseValue201ApplicationJson, SdkCreateWebhookChannelResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-webhook-channel-response-value-201-application-json.interface';
import type { SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem, SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/search-webhook-channels-response-value-200-application-json-property-data-item.interface';
import type { SdkRotateWebhookSigningSecretResponseValue200ApplicationJson, SdkRotateWebhookSigningSecretResponseValue200ApplicationJsonResponse } from './interfaces/sdk-rotate-webhook-signing-secret-response-value-200-application-json.interface';
import type { SdkGetWebhookChannelResponseValue200ApplicationJson, SdkGetWebhookChannelResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-webhook-channel-response-value-200-application-json.interface';
import type { SdkReplaceWebhookChannelResponseValue200ApplicationJson, SdkReplaceWebhookChannelResponseValue200ApplicationJsonResponse } from './interfaces/sdk-replace-webhook-channel-response-value-200-application-json.interface';
import type { SdkUpdateWebhookChannelResponseValue200ApplicationJson, SdkUpdateWebhookChannelResponseValue200ApplicationJsonResponse } from './interfaces/sdk-update-webhook-channel-response-value-200-application-json.interface';
import type { SdkSendTestWebhookResponseValue200ApplicationJson, SdkSendTestWebhookResponseValue200ApplicationJsonResponse } from './interfaces/sdk-send-test-webhook-response-value-200-application-json.interface';
import type { SdkCreateWebhookChannelRequestApplicationJsonResponse } from './interfaces/sdk-create-webhook-channel-request-application-json.interface';
import type { SdkSearchWebhookChannelsRequestApplicationJsonResponse } from './interfaces/sdk-search-webhook-channels-request-application-json.interface';
import type { SdkRotateWebhookSigningSecretRequestApplicationJsonResponse } from './interfaces/sdk-rotate-webhook-signing-secret-request-application-json.interface';
import type { SdkReplaceWebhookChannelRequestApplicationJsonResponse } from './interfaces/sdk-replace-webhook-channel-request-application-json.interface';
import type { SdkUpdateWebhookChannelRequestApplicationJsonResponse } from './interfaces/sdk-update-webhook-channel-request-application-json.interface';
import type { SdkSendTestWebhookRequestApplicationJsonResponse } from './interfaces/sdk-send-test-webhook-request-application-json.interface';
import { deserializeListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-webhook-channels-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkCreateWebhookChannelResponseValue201ApplicationJson } from './serializers/sdk-create-webhook-channel-response-value-201-application-json.serializer';
import { deserializeSearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/search-webhook-channels-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkRotateWebhookSigningSecretResponseValue200ApplicationJson } from './serializers/sdk-rotate-webhook-signing-secret-response-value-200-application-json.serializer';
import { deserializeSdkGetWebhookChannelResponseValue200ApplicationJson } from './serializers/sdk-get-webhook-channel-response-value-200-application-json.serializer';
import { deserializeSdkReplaceWebhookChannelResponseValue200ApplicationJson } from './serializers/sdk-replace-webhook-channel-response-value-200-application-json.serializer';
import { deserializeSdkUpdateWebhookChannelResponseValue200ApplicationJson } from './serializers/sdk-update-webhook-channel-response-value-200-application-json.serializer';
import { deserializeSdkSendTestWebhookResponseValue200ApplicationJson } from './serializers/sdk-send-test-webhook-response-value-200-application-json.serializer';
import { serializeSdkCreateWebhookChannelRequestApplicationJson } from './serializers/sdk-create-webhook-channel-request-application-json.serializer';
import { serializeSdkSearchWebhookChannelsRequestApplicationJson } from './serializers/sdk-search-webhook-channels-request-application-json.serializer';
import { serializeSdkRotateWebhookSigningSecretRequestApplicationJson } from './serializers/sdk-rotate-webhook-signing-secret-request-application-json.serializer';
import { serializeSdkReplaceWebhookChannelRequestApplicationJson } from './serializers/sdk-replace-webhook-channel-request-application-json.serializer';
import { serializeSdkUpdateWebhookChannelRequestApplicationJson } from './serializers/sdk-update-webhook-channel-request-application-json.serializer';
import { serializeSdkSendTestWebhookRequestApplicationJson } from './serializers/sdk-send-test-webhook-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class WebhookChannels {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List webhook channels
   *
   * Retrieve the authenticated store's outbound developer webhook destinations. Signing secrets and delivery headers are never included. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: WebhookChannelsListOptions): Promise<AutoPaginatable<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem, WebhookChannelsListOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemResponse, ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/webhook-channels',
        deserializeListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItemResponse, ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/webhook-channels',
          deserializeListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create a webhook channel
   *
   * Create an outbound webhook destination with a stable UUID and an explicit event filter. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkCreateWebhookChannelResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateWebhookChannelResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateWebhookChannelResponseValue201ApplicationJsonResponse, SdkCreateWebhookChannelRequestApplicationJsonResponse>(
      '/v2/webhook-channels',
      serializeSdkCreateWebhookChannelRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createWebhookChannel' },
    );
    return deserializeSdkCreateWebhookChannelResponseValue201ApplicationJson(data);
  }

  /**
   * Search webhook channels
   *
   * Search destinations by name or URL and optionally filter by a subscribed event. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SearchWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchWebhookChannelsResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchWebhookChannelsResponseValue200ApplicationJsonResponse, SdkSearchWebhookChannelsRequestApplicationJsonResponse>(
      '/v2/webhook-channels/search',
      serializeSdkSearchWebhookChannelsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchWebhookChannels' },
    );
    return deserializeSdkSearchWebhookChannelsResponseValue200ApplicationJson(data);
  }

  /**
   * Rotate the webhook signing secret
   *
   * Replace the store-wide outbound webhook signing secret. The value is write-only and is never returned or written to delivery logs. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkRotateWebhookSigningSecretResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async rotate(options: RotateOptions): Promise<SdkRotateWebhookSigningSecretResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkRotateWebhookSigningSecretResponseValue200ApplicationJsonResponse, SdkRotateWebhookSigningSecretRequestApplicationJsonResponse>(
      '/v2/webhook-channels/signing-secret',
      serializeSdkRotateWebhookSigningSecretRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'rotateWebhookSigningSecret' },
    );
    return deserializeSdkRotateWebhookSigningSecretResponseValue200ApplicationJson(data);
  }

  /**
   * Retrieve a webhook channel
   *
   * Retrieve one outbound webhook destination by its stable UUID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.webhookChannel - The stable webhook channel UUID.
   * @example "0f33d01f-f9f8-45e8-80c8-7734d057196d"
   * @returns {Promise<SdkGetWebhookChannelResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetWebhookChannelResponseValue200ApplicationJson> {
    const { webhookChannel } = options;
    const { data } = await this.sellApp.get<SdkGetWebhookChannelResponseValue200ApplicationJsonResponse>(`/v2/webhook-channels/${encodeURIComponent(webhookChannel)}`, { ...options.requestOptions, operationId: 'getWebhookChannel' });
    return deserializeSdkGetWebhookChannelResponseValue200ApplicationJson(data);
  }

  /**
   * Replace a webhook channel
   *
   * Replace a destination with a complete representation. name, url, and allowed_notifications must all be present. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.webhookChannel - The stable webhook channel UUID.
   * @example "0f33d01f-f9f8-45e8-80c8-7734d057196d"
   * @returns {Promise<SdkReplaceWebhookChannelResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceWebhookChannelResponseValue200ApplicationJson> {
    const { webhookChannel, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceWebhookChannelResponseValue200ApplicationJsonResponse, SdkReplaceWebhookChannelRequestApplicationJsonResponse>(
      `/v2/webhook-channels/${encodeURIComponent(webhookChannel)}`,
      serializeSdkReplaceWebhookChannelRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceWebhookChannel' },
    );
    return deserializeSdkReplaceWebhookChannelResponseValue200ApplicationJson(data);
  }

  /**
   * Update a webhook channel
   *
   * Partially update a destination. Supplying allowed_notifications replaces the complete event filter; omitted fields retain their current values. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.webhookChannel - The stable webhook channel UUID.
   * @example "0f33d01f-f9f8-45e8-80c8-7734d057196d"
   * @returns {Promise<SdkUpdateWebhookChannelResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateWebhookChannelResponseValue200ApplicationJson> {
    const { webhookChannel, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateWebhookChannelResponseValue200ApplicationJsonResponse, SdkUpdateWebhookChannelRequestApplicationJsonResponse>(
      `/v2/webhook-channels/${encodeURIComponent(webhookChannel)}`,
      serializeSdkUpdateWebhookChannelRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateWebhookChannel' },
    );
    return deserializeSdkUpdateWebhookChannelResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a webhook channel
   *
   * Delete a destination. Other channel UUIDs remain unchanged and the deleted UUID is not reassigned. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.webhookChannel - The stable webhook channel UUID.
   * @example "0f33d01f-f9f8-45e8-80c8-7734d057196d"
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { webhookChannel } = options;
    await this.sellApp.delete(`/v2/webhook-channels/${encodeURIComponent(webhookChannel)}`, { ...options.requestOptions, operationId: 'deleteWebhookChannel' });
  }

  /**
   * Send a test webhook
   *
   * Synchronously send one mock delivery to the stored destination. `event` is the public delivery event name and the same name must already be enabled in `allowed_notifications`. The test payload uses the same top-level event/version contract as production. Test deliveries do not retry or follow redirects and never return response bodies or signing headers. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.webhookChannel - The stable webhook channel UUID.
   * @example "0f33d01f-f9f8-45e8-80c8-7734d057196d"
   * @returns {Promise<SdkSendTestWebhookResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async send(options: SendOptions): Promise<SdkSendTestWebhookResponseValue200ApplicationJson> {
    const { webhookChannel, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSendTestWebhookResponseValue200ApplicationJsonResponse, SdkSendTestWebhookRequestApplicationJsonResponse>(
      `/v2/webhook-channels/${encodeURIComponent(webhookChannel)}/test`,
      serializeSdkSendTestWebhookRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'sendTestWebhook' },
    );
    return deserializeSdkSendTestWebhookResponseValue200ApplicationJson(data);
  }
}
