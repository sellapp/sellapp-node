// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { ListExportsOptions } from './interfaces/list-exports-options.interface';
import type { CreateExportOptions } from './interfaces/create-export-options.interface';
import type { GetExportOptions } from './interfaces/get-export-options.interface';
import type { DownloadExportOptions } from './interfaces/download-export-options.interface';
import type { ListExportsResponseValue200ApplicationJsonPropertyDataItem, ListExportsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-exports-response-value-200-application-json-property-data-item.interface';
import type { SdkCreateExportResponseValue201ApplicationJson, SdkCreateExportResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-export-response-value-201-application-json.interface';
import type { SdkGetExportResponseValue200ApplicationJson, SdkGetExportResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-export-response-value-200-application-json.interface';
import type { SdkCreateExportRequestApplicationJsonResponse } from './interfaces/sdk-create-export-request-application-json.interface';
import { deserializeListExportsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-exports-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkCreateExportResponseValue201ApplicationJson } from './serializers/sdk-create-export-response-value-201-application-json.serializer';
import { deserializeSdkGetExportResponseValue200ApplicationJson } from './serializers/sdk-get-export-response-value-200-application-json.serializer';
import { serializeSdkCreateExportRequestApplicationJson } from './serializers/sdk-create-export-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class Exports {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List exports
   *
   * List permitted exports, 50 per page. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request. Sales, tax and customer reports require invoice store permission. Affiliate payout reports require affiliate and affiliate:payout permissions. The category selects the applicable permission checks; listing returns only authorized categories.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListExportsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async listExports(options?: ListExportsOptions): Promise<AutoPaginatable<ListExportsResponseValue200ApplicationJsonPropertyDataItem, ListExportsOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListExportsResponseValue200ApplicationJsonPropertyDataItemResponse, ListExportsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/exports',
        deserializeListExportsResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListExportsResponseValue200ApplicationJsonPropertyDataItemResponse, ListExportsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/exports',
          deserializeListExportsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create an export
   *
   * Create a queued sales, tax, customer or affiliate payout report. One export may be queued or running per store, and two per requesting account across all credentials and stores. When capacity is full, HTTP 429 includes Retry-After: 60; poll the existing export and wait before creating another. Date ranges include both end dates and may span at most 366 calendar days. Omitted or null to defaults to today; omitted or null from defaults to 365 days before to. Sales and tax use sale update dates; customers and affiliate payouts use creation dates. Reports are limited to 10,000 source rows, 5 MiB of serialized source data and a 10 MiB output file. Tax summaries count source sales toward these limits. A report that exceeds a limit fails without publishing a partial file; request a smaller date range. Jobs may wait up to 15 minutes to start and run for at most 60 seconds. The requesting actor is recorded but is not returned in this response. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request. Sales, tax and customer reports require invoice store permission. Affiliate payout reports require affiliate and affiliate:payout permissions. The category selects the applicable permission checks; listing returns only authorized categories.
   * @returns {Promise<SdkCreateExportResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async createExport(options: CreateExportOptions): Promise<SdkCreateExportResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateExportResponseValue201ApplicationJsonResponse, SdkCreateExportRequestApplicationJsonResponse>(
      '/v2/exports',
      serializeSdkCreateExportRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createExport' },
    );
    return deserializeSdkCreateExportResponseValue201ApplicationJson(data);
  }

  /**
   * Retrieve an export
   *
   * Poll until completed, failed, or expired. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request. Sales, tax and customer reports require invoice store permission. Affiliate payout reports require affiliate and affiliate:payout permissions. The category selects the applicable permission checks; listing returns only authorized categories.
   * @param options - The request options.
   * @param options.export - The export identifier.
   * @example "01992a65-e064-71ba-b38f-902b7966a6be"
   * @returns {Promise<SdkGetExportResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async getExport(options: GetExportOptions): Promise<SdkGetExportResponseValue200ApplicationJson> {
    const { export: exportValue } = options;
    const { data } = await this.sellApp.get<SdkGetExportResponseValue200ApplicationJsonResponse>(`/v2/exports/${encodeURIComponent(exportValue)}`, { ...options.requestOptions, operationId: 'getExport' });
    return deserializeSdkGetExportResponseValue200ApplicationJson(data);
  }

  /**
   * Download an export
   *
   * Validate the five-minute signed API URL, then redirect to a short-lived private object-storage URL. The API does not stream the file. Pending or running exports return 409; expired exports return 410. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request. Sales, tax and customer reports require invoice store permission. Affiliate payout reports require affiliate and affiliate:payout permissions. The category selects the applicable permission checks; listing returns only authorized categories.
   * @param options - Additional query options.
   * @param options.export - The export identifier.
   * @example "01992a65-e064-71ba-b38f-902b7966a6be"
   * @param options.expires - Expiry timestamp from the generated download URL.
   * @example 1788513423
   * @param options.signature - Copy the signature from the response-supplied download_url. The example is illustrative and cannot authorize a download.
   * @example "2c91df645a086ec399153a932b741f809d2b85c69740eaf3612384ebfb913a65"
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async downloadExport(options: DownloadExportOptions): Promise<void> {
    const { export: exportValue } = options;
    await this.sellApp.get(`/v2/exports/${encodeURIComponent(exportValue)}/download`, { ...options.requestOptions, operationId: 'downloadExport', query: { expires: options.expires, signature: options.signature } });
  }
}
