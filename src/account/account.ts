// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { CreateStoreOptions } from './interfaces/create-store-options.interface';
import type { GetStoreOptions } from './interfaces/get-store-options.interface';
import type { SdkGetAccountResponseValue200ApplicationJson, SdkGetAccountResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-account-response-value-200-application-json.interface';
import type { SdkListStoresResponseValue200ApplicationJson, SdkListStoresResponseValue200ApplicationJsonResponse } from './interfaces/sdk-list-stores-response-value-200-application-json.interface';
import type { SdkCreateStoreResponseValue201ApplicationJson, SdkCreateStoreResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-store-response-value-201-application-json.interface';
import type { SdkGetStoreResponseValue200ApplicationJson, SdkGetStoreResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-store-response-value-200-application-json.interface';
import type { SdkGetStorePermissionsResponseValue200ApplicationJson, SdkGetStorePermissionsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-store-permissions-response-value-200-application-json.interface';
import type { SdkCreateStoreRequestApplicationJsonResponse } from './interfaces/sdk-create-store-request-application-json.interface';
import { deserializeSdkGetAccountResponseValue200ApplicationJson } from './serializers/sdk-get-account-response-value-200-application-json.serializer';
import { deserializeSdkListStoresResponseValue200ApplicationJson } from './serializers/sdk-list-stores-response-value-200-application-json.serializer';
import { deserializeSdkCreateStoreResponseValue201ApplicationJson } from './serializers/sdk-create-store-response-value-201-application-json.serializer';
import { deserializeSdkGetStoreResponseValue200ApplicationJson } from './serializers/sdk-get-store-response-value-200-application-json.serializer';
import { deserializeSdkGetStorePermissionsResponseValue200ApplicationJson } from './serializers/sdk-get-store-permissions-response-value-200-application-json.serializer';
import { serializeSdkCreateStoreRequestApplicationJson } from './serializers/sdk-create-store-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class Account {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * Read your profile
   *
   * Read your account identity and credential type with account:read on an API key or the official CLI OAuth grant. No store or X-STORE is needed, including before creating your first store. Browser and customer sessions are rejected.
   * @returns {Promise<SdkGetAccountResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async getAccount(requestOptions: RequestOptions = {}): Promise<SdkGetAccountResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkGetAccountResponseValue200ApplicationJsonResponse>('/v2/me', requestOptions);
    return deserializeSdkGetAccountResponseValue200ApplicationJson(data);
  }

  /**
   * List accessible stores
   *
   * List all stores you can currently access, including stores created or joined after login. API keys require account:read; selected-store keys see only the intersection of current membership and their fixed store list. An empty list is a successful result. Use a returned slug in X-STORE for store operations.
   * @returns {Promise<SdkListStoresResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async listStores(requestOptions: RequestOptions = {}): Promise<SdkListStoresResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkListStoresResponseValue200ApplicationJsonResponse>('/v2/stores', requestOptions);
    return deserializeSdkListStoresResponseValue200ApplicationJson(data);
  }

  /**
   * Create a store
   *
   * Create a store owned by the authenticated user, with the same defaults as dashboard creation. Requires stores:create on an unrestricted user-owned API key, or the official CLI OAuth admin grant. Customer and browser sessions and direct MCP bearer tokens are rejected; MCP uses reviewed server delegation. Selected-store keys cannot create stores. No existing store or X-STORE header is needed; Name and slug are required; slug is normalized to lowercase. Visibility defaults to PUBLIC. This does not change your dashboard's selected store. Idempotency-Key is required and retained for 24 hours: reuse the same key and identical body after a lost response. A conflicting request returns 409; invalid or duplicate slugs return 422. Retries are bound to the user and operation across token rotation and supported credential types; current authorization is checked before replay. Store defaults and the successful retry receipt commit together. You can create one new store per user every 60 seconds, across all credentials. A new creation during that interval returns 429 with Retry-After and rate-limit headers. Wait for that delay and retry the same key and body. Replaying a successful original key recovers its store without creating another or consuming another creation allowance.
   * @returns {Promise<SdkCreateStoreResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async createStore(options: CreateStoreOptions): Promise<SdkCreateStoreResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateStoreResponseValue201ApplicationJsonResponse, SdkCreateStoreRequestApplicationJsonResponse>(
      '/v2/stores',
      serializeSdkCreateStoreRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createStore' },
    );
    return deserializeSdkCreateStoreResponseValue201ApplicationJson(data);
  }

  /**
   * Read an accessible store
   *
   * Read a currently accessible store by numeric ID with account:read on an API key or official CLI OAuth. No X-STORE is needed. Keys with selected stores must include this ID. Inaccessible stores return 404.
   * @example 12
   * @returns {Promise<SdkGetStoreResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async getStore(options: GetStoreOptions): Promise<SdkGetStoreResponseValue200ApplicationJson> {
    const { store } = options;
    const { data } = await this.sellApp.get<SdkGetStoreResponseValue200ApplicationJsonResponse>(`/v2/stores/${encodeURIComponent(store)}`, { ...options.requestOptions, operationId: 'getStore' });
    return deserializeSdkGetStoreResponseValue200ApplicationJson(data);
  }

  /**
   * Inspect effective permissions
   *
   * Inspect authorization for the explicit X-STORE slug. API keys require account:read and return key_abilities, store_permissions, effective_abilities and store_access. OAuth returns its granted scopes and reviewed permission categories. The credential_type identifies the typed response. These checks do not establish record-state, provider or financial eligibility.
   * @returns {Promise<SdkGetStorePermissionsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async getStorePermissions(requestOptions: RequestOptions = {}): Promise<SdkGetStorePermissionsResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkGetStorePermissionsResponseValue200ApplicationJsonResponse>('/v2/permissions', requestOptions);
    return deserializeSdkGetStorePermissionsResponseValue200ApplicationJson(data);
  }
}
