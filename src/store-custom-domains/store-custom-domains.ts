// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { ConnectOptions } from './interfaces/connect-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { DisconnectOptions } from './interfaces/disconnect-options.interface';
import type { RefreshOptions } from './interfaces/refresh-options.interface';
import type { SdkListCustomDomainsResponseValue200ApplicationJson, SdkListCustomDomainsResponseValue200ApplicationJsonResponse } from '../store-settings/interfaces/sdk-list-custom-domains-response-value-200-application-json.interface';
import type { SdkConnectCustomDomainResponseValue201ApplicationJson, SdkConnectCustomDomainResponseValue201ApplicationJsonResponse } from '../store-settings/interfaces/sdk-connect-custom-domain-response-value-201-application-json.interface';
import type { SdkGetCustomDomainResponseValue200ApplicationJson, SdkGetCustomDomainResponseValue200ApplicationJsonResponse } from '../store-settings/interfaces/sdk-get-custom-domain-response-value-200-application-json.interface';
import type { SdkRefreshCustomDomainStatusResponseValue200ApplicationJson, SdkRefreshCustomDomainStatusResponseValue200ApplicationJsonResponse } from '../store-settings/interfaces/sdk-refresh-custom-domain-status-response-value-200-application-json.interface';
import type { SdkConnectCustomDomainRequestApplicationJsonResponse } from '../store-settings/interfaces/sdk-connect-custom-domain-request-application-json.interface';
import { deserializeSdkListCustomDomainsResponseValue200ApplicationJson } from '../store-settings/serializers/sdk-list-custom-domains-response-value-200-application-json.serializer';
import { deserializeSdkConnectCustomDomainResponseValue201ApplicationJson } from '../store-settings/serializers/sdk-connect-custom-domain-response-value-201-application-json.serializer';
import { deserializeSdkGetCustomDomainResponseValue200ApplicationJson } from '../store-settings/serializers/sdk-get-custom-domain-response-value-200-application-json.serializer';
import { deserializeSdkRefreshCustomDomainStatusResponseValue200ApplicationJson } from '../store-settings/serializers/sdk-refresh-custom-domain-status-response-value-200-application-json.serializer';
import { serializeSdkConnectCustomDomainRequestApplicationJson } from '../store-settings/serializers/sdk-connect-custom-domain-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class StoreCustomDomains {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List custom domains
   *
   * List the current store's custom-domain connection states and DNS verification records. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkListCustomDomainsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(requestOptions: RequestOptions = {}): Promise<SdkListCustomDomainsResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkListCustomDomainsResponseValue200ApplicationJsonResponse>('/v2/store/custom-domains', requestOptions);
    return deserializeSdkListCustomDomainsResponseValue200ApplicationJson(data);
  }

  /**
   * Connect a custom domain
   *
   * Connect a hostname through SellApp's managed Cloudflare custom-hostname flow. IP addresses, wildcard domains, credentials, ports, queries, and fragments are rejected. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkConnectCustomDomainResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async connect(options: ConnectOptions): Promise<SdkConnectCustomDomainResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkConnectCustomDomainResponseValue201ApplicationJsonResponse, SdkConnectCustomDomainRequestApplicationJsonResponse>(
      '/v2/store/custom-domains',
      serializeSdkConnectCustomDomainRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'connectCustomDomain' },
    );
    return deserializeSdkConnectCustomDomainResponseValue201ApplicationJson(data);
  }

  /**
   * Retrieve a custom domain
   *
   * Retrieve a custom domain. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customDomain - The customDomain path parameter.
   * @returns {Promise<SdkGetCustomDomainResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetCustomDomainResponseValue200ApplicationJson> {
    const { customDomain } = options;
    const { data } = await this.sellApp.get<SdkGetCustomDomainResponseValue200ApplicationJsonResponse>(`/v2/store/custom-domains/${encodeURIComponent(customDomain)}`, { ...options.requestOptions, operationId: 'getCustomDomain' });
    return deserializeSdkGetCustomDomainResponseValue200ApplicationJson(data);
  }

  /**
   * Disconnect a custom domain
   *
   * Disconnect a custom domain. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customDomain - The customDomain path parameter.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async disconnect(options: DisconnectOptions): Promise<void> {
    const { customDomain } = options;
    await this.sellApp.delete(`/v2/store/custom-domains/${encodeURIComponent(customDomain)}`, { ...options.requestOptions, operationId: 'disconnectCustomDomain' });
  }

  /**
   * Refresh custom domain status
   *
   * Ask Cloudflare to re-run custom-hostname validation and persist the latest DNS and SSL state. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customDomain - The customDomain path parameter.
   * @returns {Promise<SdkRefreshCustomDomainStatusResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async refresh(options: RefreshOptions): Promise<SdkRefreshCustomDomainStatusResponseValue200ApplicationJson> {
    const { customDomain } = options;
    const { data } = await this.sellApp.post<SdkRefreshCustomDomainStatusResponseValue200ApplicationJsonResponse>(`/v2/store/custom-domains/${encodeURIComponent(customDomain)}/refresh`, {}, { ...options.requestOptions, operationId: 'refreshCustomDomainStatus' });
    return deserializeSdkRefreshCustomDomainStatusResponseValue200ApplicationJson(data);
  }
}
