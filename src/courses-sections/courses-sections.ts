// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { ReorderOptions } from './interfaces/reorder-options.interface';
import type { SdkCreateCourseSectionResponseValue201ApplicationJson, SdkCreateCourseSectionResponseValue201ApplicationJsonResponse } from '../courses/interfaces/sdk-create-course-section-response-value-201-application-json.interface';
import type { SdkReplaceCourseSectionResponseValue200ApplicationJson, SdkReplaceCourseSectionResponseValue200ApplicationJsonResponse } from '../courses/interfaces/sdk-replace-course-section-response-value-200-application-json.interface';
import type { SdkUpdateCourseSectionResponseValue200ApplicationJson, SdkUpdateCourseSectionResponseValue200ApplicationJsonResponse } from '../courses/interfaces/sdk-update-course-section-response-value-200-application-json.interface';
import type { SdkReorderCourseSectionsResponseValue200ApplicationJson, SdkReorderCourseSectionsResponseValue200ApplicationJsonResponse } from '../courses/interfaces/sdk-reorder-course-sections-response-value-200-application-json.interface';
import type { SdkCreateCourseSectionRequestApplicationJsonResponse } from '../courses/interfaces/sdk-create-course-section-request-application-json.interface';
import type { SdkReplaceCourseSectionRequestApplicationJsonResponse } from '../courses/interfaces/sdk-replace-course-section-request-application-json.interface';
import type { SdkUpdateCourseSectionRequestApplicationJsonResponse } from '../courses/interfaces/sdk-update-course-section-request-application-json.interface';
import type { SdkReorderCourseSectionsRequestApplicationJsonResponse } from '../courses/interfaces/sdk-reorder-course-sections-request-application-json.interface';
import { deserializeSdkCreateCourseSectionResponseValue201ApplicationJson } from '../courses/serializers/sdk-create-course-section-response-value-201-application-json.serializer';
import { deserializeSdkReplaceCourseSectionResponseValue200ApplicationJson } from '../courses/serializers/sdk-replace-course-section-response-value-200-application-json.serializer';
import { deserializeSdkUpdateCourseSectionResponseValue200ApplicationJson } from '../courses/serializers/sdk-update-course-section-response-value-200-application-json.serializer';
import { deserializeSdkReorderCourseSectionsResponseValue200ApplicationJson } from '../courses/serializers/sdk-reorder-course-sections-response-value-200-application-json.serializer';
import { serializeSdkCreateCourseSectionRequestApplicationJson } from '../courses/serializers/sdk-create-course-section-request-application-json.serializer';
import { serializeSdkReplaceCourseSectionRequestApplicationJson } from '../courses/serializers/sdk-replace-course-section-request-application-json.serializer';
import { serializeSdkUpdateCourseSectionRequestApplicationJson } from '../courses/serializers/sdk-update-course-section-request-application-json.serializer';
import { serializeSdkReorderCourseSectionsRequestApplicationJson } from '../courses/serializers/sdk-reorder-course-sections-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class CoursesSections {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * Create a course section
   *
   * Append a section to a course curriculum. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @returns {Promise<SdkCreateCourseSectionResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateCourseSectionResponseValue201ApplicationJson> {
    const { course, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateCourseSectionResponseValue201ApplicationJsonResponse, SdkCreateCourseSectionRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/sections`,
      serializeSdkCreateCourseSectionRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createCourseSection' },
    );
    return deserializeSdkCreateCourseSectionResponseValue201ApplicationJson(data);
  }

  /**
   * Update a course section
   *
   * Update a section title or sanitized rich-text description. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.section - The section path parameter.
   * @returns {Promise<SdkReplaceCourseSectionResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceCourseSectionResponseValue200ApplicationJson> {
    const { course, section, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceCourseSectionResponseValue200ApplicationJsonResponse, SdkReplaceCourseSectionRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/sections/${encodeURIComponent(section)}`,
      serializeSdkReplaceCourseSectionRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceCourseSection' },
    );
    return deserializeSdkReplaceCourseSectionResponseValue200ApplicationJson(data);
  }

  /**
   * Update a course section
   *
   * Update a section title or sanitized rich-text description. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.section - The section path parameter.
   * @returns {Promise<SdkUpdateCourseSectionResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateCourseSectionResponseValue200ApplicationJson> {
    const { course, section, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateCourseSectionResponseValue200ApplicationJsonResponse, SdkUpdateCourseSectionRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/sections/${encodeURIComponent(section)}`,
      serializeSdkUpdateCourseSectionRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateCourseSection' },
    );
    return deserializeSdkUpdateCourseSectionResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a course section
   *
   * Delete a section, its lessons, structured questions, attachments, and video references, then normalize remaining section order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.section - The section path parameter.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { course, section } = options;
    await this.sellApp.delete(`/v2/courses/${encodeURIComponent(course)}/sections/${encodeURIComponent(section)}`, { ...options.requestOptions, operationId: 'deleteCourseSection' });
  }

  /**
   * Reorder course sections
   *
   * Replace the complete section order. Send every section ID exactly once in the desired order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @returns {Promise<SdkReorderCourseSectionsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async reorder(options: ReorderOptions): Promise<SdkReorderCourseSectionsResponseValue200ApplicationJson> {
    const { course, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkReorderCourseSectionsResponseValue200ApplicationJsonResponse, SdkReorderCourseSectionsRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/sections/reorder`,
      serializeSdkReorderCourseSectionsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'reorderCourseSections' },
    );
    return deserializeSdkReorderCourseSectionsResponseValue200ApplicationJson(data);
  }
}
