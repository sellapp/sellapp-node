// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { GetOAuthAuthorizationRequestOptions } from './interfaces/get-oauth-authorization-request-options.interface';
import type { ApproveOAuthAuthorizationOptions } from './interfaces/approve-oauth-authorization-options.interface';
import type { DenyOAuthAuthorizationOptions } from './interfaces/deny-oauth-authorization-options.interface';
import type { ExchangeOAuthTokenOptions } from './interfaces/exchange-oauth-token-options.interface';
import type { RevokeOAuthTokenOptions } from './interfaces/revoke-oauth-token-options.interface';
import type { SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson, SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-oauth-authorization-server-metadata-response-value-200-application-json.interface';
import type { SdkExchangeOAuthTokenResponseValue200ApplicationJson, SdkExchangeOAuthTokenResponseValue200ApplicationJsonResponse } from './interfaces/sdk-exchange-oauth-token-response-value-200-application-json.interface';
import type { SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencodedResponse } from './interfaces/sdk-approve-oauth-authorization-request-application-x-www-form-urlencoded.interface';
import type { SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencodedResponse } from './interfaces/sdk-deny-oauth-authorization-request-application-x-www-form-urlencoded.interface';
import type { ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1Response } from './interfaces/exchange-oauth-token-request-application-x-www-form-urlencoded-one-of-value-1.interface';
import type { ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2Response } from './interfaces/exchange-oauth-token-request-application-x-www-form-urlencoded-one-of-value-2.interface';
import type { SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedResponse } from './interfaces/sdk-revoke-oauth-token-request-application-x-www-form-urlencoded.interface';
import { deserializeSdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson } from './serializers/sdk-get-oauth-authorization-server-metadata-response-value-200-application-json.serializer';
import { deserializeSdkExchangeOAuthTokenResponseValue200ApplicationJson } from './serializers/sdk-exchange-oauth-token-response-value-200-application-json.serializer';
import { serializeSdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded } from './serializers/sdk-approve-oauth-authorization-request-application-x-www-form-urlencoded.serializer';
import { serializeSdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded } from './serializers/sdk-deny-oauth-authorization-request-application-x-www-form-urlencoded.serializer';
import { serializeExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1 } from './serializers/exchange-oauth-token-request-application-x-www-form-urlencoded-one-of-value-1.serializer';
import { serializeExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2 } from './serializers/exchange-oauth-token-request-application-x-www-form-urlencoded-one-of-value-2.serializer';
import { serializeSdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded } from './serializers/sdk-revoke-oauth-token-request-application-x-www-form-urlencoded.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class OAuth {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * Read OAuth server metadata
   *
   * Discover the official CLI authorization, token and revocation endpoints and supported scopes. The CLI uses public client authentication without a secret.
   * @returns {Promise<SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson>}
   * @throws {RateLimitExceededException} 429
   */
  async getOAuthAuthorizationServerMetadata(requestOptions: RequestOptions = {}): Promise<SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJsonResponse>('/.well-known/oauth-authorization-server', requestOptions);
    return deserializeSdkGetOAuthAuthorizationServerMetadataResponseValue200ApplicationJson(data);
  }

  /**
   * Review CLI authorization
   *
   * Open in the user's browser to authorize the official CLI using existing store permissions. Only the configured public CLI registration is supported. PKCE S256 and non-empty state are required. The callback may vary only the port of the registered numeric loopback URL; localhost, wildcard hosts, fragments and non-loopback HTTP are rejected. Unsupported clients receive invalid_client and unsupported scopes receive invalid_scope.
   * @example "code"
   * @example "01992a65-e064-71ba-b38f-902b7966a6be"
   * @example "http://127.0.0.1:49152/callback"
   * @example "admin"
   * @example "RANDOM_STATE"
   * @example "E9Melhoa2OwvFrEMTJguCHaoeK1t8URWbuGJSstw-cM"
   * @example "S256"
   * @param options - Additional query options.
   * @returns {Promise<string>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {RateLimitExceededException} 429
   */
  async getOAuthAuthorizationRequest(options: GetOAuthAuthorizationRequestOptions): Promise<string> {
    const { data } = await this.sellApp.get<string>('/oauth/authorize', { ...options.requestOptions, operationId: 'getOAuthAuthorizationRequest', query: { response_type: options.responseType, client_id: options.clientId, redirect_uri: options.redirectUri, ...(options.scope !== undefined && { scope: options.scope }), state: options.state, code_challenge: options.codeChallenge, code_challenge_method: options.codeChallengeMethod } });
    return data;
  }

  /**
   * Approve CLI access
   *
   * Submit the official CLI consent form in the same logged-in browser session. The admin grant covers every store you can access now or later, including when you currently have no stores. It also permits store creation. Local agents connected through the CLI share this connection. Approval never adds store role permissions. Current client eligibility, membership and permissions are rechecked, including for stale consent.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async approveOAuthAuthorization(options: ApproveOAuthAuthorizationOptions): Promise<void> {
    const { requestOptions, ...payload } = options;
    await this.sellApp.post<void, SdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencodedResponse>('/oauth/authorize', serializeSdkApproveOAuthAuthorizationRequestApplicationXWwwFormUrlencoded(payload), { ...options.requestOptions, operationId: 'approveOAuthAuthorization' });
  }

  /**
   * Deny CLI access
   *
   * Submit the denial form from the same browser session. The callback receives access_denied and the original state.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {RateLimitExceededException} 429
   */
  async denyOAuthAuthorization(options: DenyOAuthAuthorizationOptions): Promise<void> {
    const { requestOptions, ...payload } = options;
    await this.sellApp.deleteWithBody<SdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencodedResponse>('/oauth/authorize', serializeSdkDenyOAuthAuthorizationRequestApplicationXWwwFormUrlencoded(payload), { ...options.requestOptions, operationId: 'denyOAuthAuthorization' });
  }

  /**
   * Exchange or refresh OAuth tokens
   *
   * Exchange an official CLI authorization code with the original redirect URI and PKCE verifier, or rotate a refresh token. Send the configured client_id without a secret. Client eligibility and current grant validity are rechecked. Access tokens last 60 minutes; refresh token families last 90 days. Atomically replace rotated refresh tokens. Reuse may revoke the family; reauthorize after invalid_grant. Fix registration or scope configuration for invalid_client or invalid_scope instead of repeating login.
   * @returns {Promise<SdkExchangeOAuthTokenResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {RateLimitExceededException} 429
   */
  async exchangeOAuthToken(options: ExchangeOAuthTokenOptions): Promise<SdkExchangeOAuthTokenResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkExchangeOAuthTokenResponseValue200ApplicationJsonResponse, ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1Response | ExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2Response>(
      '/oauth/token',
      (() => { switch (payload.grantType) { case 'authorization_code': return serializeExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue1(payload); case 'refresh_token': return serializeExchangeOAuthTokenRequestApplicationXWwwFormUrlencodedOneOfValue2(payload); default: { const _unknown: never = payload; throw new Error(`Unknown grantType: ${(_unknown as { grantType?: unknown }).grantType}`) } } })(), { ...options.requestOptions, operationId: 'exchangeOAuthToken' },
    );
    return deserializeSdkExchangeOAuthTokenResponseValue200ApplicationJson(data);
  }

  /**
   * Revoke an OAuth token
   *
   * Revoke the matching personal CLI installation and all its token families. Send the configured public client_id without a secret. Unknown tokens also succeed without disclosing whether a token existed.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {RateLimitExceededException} 429
   */
  async revokeOAuthToken(options: RevokeOAuthTokenOptions): Promise<void> {
    const { requestOptions, ...payload } = options;
    await this.sellApp.post<void, SdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencodedResponse>('/oauth/revoke', serializeSdkRevokeOAuthTokenRequestApplicationXWwwFormUrlencoded(payload), { ...options.requestOptions, operationId: 'revokeOAuthToken' });
  }
}
