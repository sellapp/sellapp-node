// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { StartOptions } from './interfaces/start-options.interface';
import type { PollOptions } from './interfaces/poll-options.interface';
import type { CompleteOptions } from './interfaces/complete-options.interface';
import type { VerifyOptions } from './interfaces/verify-options.interface';
import type { DisconnectOptions } from './interfaces/disconnect-options.interface';
import type { SdkListCommunityConnectionsResponseValue200ApplicationJson, SdkListCommunityConnectionsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-list-community-connections-response-value-200-application-json.interface';
import type { SdkStartCommunityConnectionResponseValue200ApplicationJson, SdkStartCommunityConnectionResponseValue200ApplicationJsonResponse } from './interfaces/sdk-start-community-connection-response-value-200-application-json.interface';
import type { SdkPollCommunityConnectionResponseValue200ApplicationJson, SdkPollCommunityConnectionResponseValue200ApplicationJsonResponse } from './interfaces/sdk-poll-community-connection-response-value-200-application-json.interface';
import type { SdkCompleteCommunityConnectionResponseValue200ApplicationJson, SdkCompleteCommunityConnectionResponseValue200ApplicationJsonResponse } from './interfaces/sdk-complete-community-connection-response-value-200-application-json.interface';
import type { SdkVerifyCommunityConnectionResponseValue200ApplicationJson, SdkVerifyCommunityConnectionResponseValue200ApplicationJsonResponse } from './interfaces/sdk-verify-community-connection-response-value-200-application-json.interface';
import type { SdkStartCommunityConnectionRequestApplicationJsonResponse } from './interfaces/sdk-start-community-connection-request-application-json.interface';
import type { SdkCompleteCommunityConnectionRequestApplicationJsonResponse } from './interfaces/sdk-complete-community-connection-request-application-json.interface';
import { deserializeSdkListCommunityConnectionsResponseValue200ApplicationJson } from './serializers/sdk-list-community-connections-response-value-200-application-json.serializer';
import { deserializeSdkStartCommunityConnectionResponseValue200ApplicationJson } from './serializers/sdk-start-community-connection-response-value-200-application-json.serializer';
import { deserializeSdkPollCommunityConnectionResponseValue200ApplicationJson } from './serializers/sdk-poll-community-connection-response-value-200-application-json.serializer';
import { deserializeSdkCompleteCommunityConnectionResponseValue200ApplicationJson } from './serializers/sdk-complete-community-connection-response-value-200-application-json.serializer';
import { deserializeSdkVerifyCommunityConnectionResponseValue200ApplicationJson } from './serializers/sdk-verify-community-connection-response-value-200-application-json.serializer';
import { serializeSdkStartCommunityConnectionRequestApplicationJson } from './serializers/sdk-start-community-connection-request-application-json.serializer';
import { serializeSdkCompleteCommunityConnectionRequestApplicationJson } from './serializers/sdk-complete-community-connection-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class CommunityConnections {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List community connections
   *
   * List Discord, Telegram, Slack, and WhatsApp connection and persisted health status for the authenticated store without contacting providers. Credentials and provider secrets are never returned. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkListCommunityConnectionsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(requestOptions: RequestOptions = {}): Promise<SdkListCommunityConnectionsResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkListCommunityConnectionsResponseValue200ApplicationJsonResponse>('/v2/community-connections', requestOptions);
    return deserializeSdkListCommunityConnectionsResponseValue200ApplicationJson(data);
  }

  /**
   * Start a community connection
   *
   * Start the provider-specific connection flow. OAuth providers return a connect URL containing encrypted state bound to the authenticated actor, store, platform, and purpose; that state expires after 15 minutes and securely authorizes the provider callback without forwarding the API bearer token or requiring a SellApp dashboard session. Open the URL exactly as issued, then poll the connection list with the original bearer token after the provider redirects. Do not append credentials, reconstruct, or reuse the URL. Legacy unsigned Slack URLs are rejected, so clients must call this endpoint again to restart those flows. Telegram returns a short-lived verification token, and WhatsApp returns a pollable QR session. The provider application must be configured, and Discord server connections require a connected Discord account. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.platform - The community platform.
   * @example "discord"
   * @returns {Promise<SdkStartCommunityConnectionResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async start(options: StartOptions): Promise<SdkStartCommunityConnectionResponseValue200ApplicationJson> {
    const { platform, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkStartCommunityConnectionResponseValue200ApplicationJsonResponse, SdkStartCommunityConnectionRequestApplicationJsonResponse>(
      `/v2/community-connections/${encodeURIComponent(platform)}/connect`,
      serializeSdkStartCommunityConnectionRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'startCommunityConnection' },
    );
    return deserializeSdkStartCommunityConnectionResponseValue200ApplicationJson(data);
  }

  /**
   * Poll a community connection
   *
   * Poll a pending Telegram verification or WhatsApp QR connection using the encrypted status token returned by the connect operation. Tokens are bound to the authenticated store and initiating user. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.platform - The community platform.
   * @returns {Promise<SdkPollCommunityConnectionResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async poll(options: PollOptions): Promise<SdkPollCommunityConnectionResponseValue200ApplicationJson> {
    const { platform } = options;
    const { data } = await this.sellApp.get<SdkPollCommunityConnectionResponseValue200ApplicationJsonResponse>(`/v2/community-connections/${encodeURIComponent(platform)}/status`, { ...options.requestOptions, operationId: 'pollCommunityConnection', query: { status_token: options.statusToken } });
    return deserializeSdkPollCommunityConnectionResponseValue200ApplicationJson(data);
  }

  /**
   * Complete a community connection
   *
   * Complete an authenticated WhatsApp QR connection by selecting a group returned by the status operation. The encrypted status token binds the session to the authenticated store and initiating user, and the server ID is revalidated against the provider response. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.platform - The community platform.
   * @example "whatsapp"
   * @returns {Promise<SdkCompleteCommunityConnectionResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async complete(options: CompleteOptions): Promise<SdkCompleteCommunityConnectionResponseValue200ApplicationJson> {
    const { platform, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCompleteCommunityConnectionResponseValue200ApplicationJsonResponse, SdkCompleteCommunityConnectionRequestApplicationJsonResponse>(
      `/v2/community-connections/${encodeURIComponent(platform)}/complete`,
      serializeSdkCompleteCommunityConnectionRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'completeCommunityConnection' },
    );
    return deserializeSdkCompleteCommunityConnectionResponseValue200ApplicationJson(data);
  }

  /**
   * Verify a community connection
   *
   * Recheck provider and server health, then safely requeue eligible failed community grants. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.platform - The community platform.
   * @returns {Promise<SdkVerifyCommunityConnectionResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async verify(options: VerifyOptions): Promise<SdkVerifyCommunityConnectionResponseValue200ApplicationJson> {
    const { platform } = options;
    const { data } = await this.sellApp.post<SdkVerifyCommunityConnectionResponseValue200ApplicationJsonResponse>(`/v2/community-connections/${encodeURIComponent(platform)}/verify`, {}, { ...options.requestOptions, operationId: 'verifyCommunityConnection' });
    return deserializeSdkVerifyCommunityConnectionResponseValue200ApplicationJson(data);
  }

  /**
   * Disconnect a community platform
   *
   * Idempotently disconnect the selected platform for the authenticated store and remove only that store's associated servers, channels, groups, or account links. Requires the `community` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.platform - The community platform.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async disconnect(options: DisconnectOptions): Promise<void> {
    const { platform } = options;
    await this.sellApp.delete(`/v2/community-connections/${encodeURIComponent(platform)}`, { ...options.requestOptions, operationId: 'disconnectCommunityPlatform' });
  }
}
