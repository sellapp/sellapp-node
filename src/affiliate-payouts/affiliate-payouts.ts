// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { AffiliatePayoutsListOptions } from './interfaces/affiliate-payouts-list-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem, ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-affiliate-payouts-response-value-200-application-json-property-data-item.interface';
import type { SdkGetAffiliatePayoutResponseValue200ApplicationJson, SdkGetAffiliatePayoutResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-affiliate-payout-response-value-200-application-json.interface';
import type { SdkCreateAffiliatePayoutResponseValue200ApplicationJson, SdkCreateAffiliatePayoutResponseValue200ApplicationJsonResponse } from './interfaces/sdk-create-affiliate-payout-response-value-200-application-json.interface';
import type { SdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJson, SdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJsonResponse } from './interfaces/sdk-update-affiliate-payout-status-response-value-200-application-json.interface';
import type { SdkUpdateAffiliatePayoutStatusRequestApplicationJsonResponse } from './interfaces/sdk-update-affiliate-payout-status-request-application-json.interface';
import { deserializeListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-affiliate-payouts-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkGetAffiliatePayoutResponseValue200ApplicationJson } from './serializers/sdk-get-affiliate-payout-response-value-200-application-json.serializer';
import { deserializeSdkCreateAffiliatePayoutResponseValue200ApplicationJson } from './serializers/sdk-create-affiliate-payout-response-value-200-application-json.serializer';
import { deserializeSdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJson } from './serializers/sdk-update-affiliate-payout-status-response-value-200-application-json.serializer';
import { serializeSdkUpdateAffiliatePayoutStatusRequestApplicationJson } from './serializers/sdk-update-affiliate-payout-status-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

const serializeAffiliatePayoutsListOptions = (options: AffiliatePayoutsListOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.affiliateId !== undefined) wire.affiliate_id = options.affiliateId;
  if (options.status !== undefined) wire.status = options.status;
  if (options.page !== undefined) wire.page = options.page;
  return wire as PaginationOptions;
};

export class AffiliatePayouts {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List affiliate payouts
   *
   * List merchant-managed affiliate payout records. These record commission payments arranged by the merchant; SellApp does not transfer or hold the funds. Requires the `affiliate` credential ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: AffiliatePayoutsListOptions): Promise<AutoPaginatable<ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItemResponse, ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/affiliate-payouts',
        deserializeListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem,
        options ? serializeAffiliatePayoutsListOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItemResponse, ListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/affiliate-payouts',
          deserializeListAffiliatePayoutsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      options ? serializeAffiliatePayoutsListOptions(options) : undefined,
    );
  }

  /**
   * Retrieve an affiliate payout
   *
   * Retrieve one merchant-managed affiliate payout record with a masked destination and referral count. Requires the `affiliate` credential ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.payout - The payout path parameter.
   * @returns {Promise<SdkGetAffiliatePayoutResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetAffiliatePayoutResponseValue200ApplicationJson> {
    const { payout } = options;
    const { data } = await this.sellApp.get<SdkGetAffiliatePayoutResponseValue200ApplicationJsonResponse>(`/v2/affiliate-payouts/${encodeURIComponent(payout)}`, { ...options.requestOptions, operationId: 'getAffiliatePayout' });
    return deserializeSdkGetAffiliatePayoutResponseValue200ApplicationJson(data);
  }

  /**
   * Create an affiliate payout
   *
   * Create a payout record for all eligible accepted referrals of one active affiliate. The merchant arranges payment separately. Replaying the same Idempotency-Key returns the same record. Requires `affiliate`, `affiliate:payout`, and enough eligible commissions to meet the store's minimum payout amount. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.affiliate - The affiliate path parameter.
   * @returns {Promise<SdkCreateAffiliatePayoutResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateAffiliatePayoutResponseValue200ApplicationJson> {
    const { affiliate } = options;
    const { data } = await this.sellApp.post<SdkCreateAffiliatePayoutResponseValue200ApplicationJsonResponse>(`/v2/affiliates/${encodeURIComponent(affiliate)}/payouts`, {}, { ...options.requestOptions, operationId: 'createAffiliatePayout' });
    return deserializeSdkCreateAffiliatePayoutResponseValue200ApplicationJson(data);
  }

  /**
   * Update affiliate payout status
   *
   * Mark an affiliate payout record due or paid and update its assigned referrals. Marking paid records a payment arranged by the merchant; it does not transfer money. Identical retries make no further change. Requires `affiliate` and `affiliate:payout`. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.payout - The payout path parameter.
   * @returns {Promise<SdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJson> {
    const { payout, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJsonResponse, SdkUpdateAffiliatePayoutStatusRequestApplicationJsonResponse>(
      `/v2/affiliate-payouts/${encodeURIComponent(payout)}/status`,
      serializeSdkUpdateAffiliatePayoutStatusRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateAffiliatePayoutStatus' },
    );
    return deserializeSdkUpdateAffiliatePayoutStatusResponseValue200ApplicationJson(data);
  }
}
