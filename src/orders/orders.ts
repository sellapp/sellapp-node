// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { OrdersListOptions } from './interfaces/orders-list-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { UpdateStatusOptions } from './interfaces/update-status-options.interface';
import type { CreateCheckoutOptions } from './interfaces/create-checkout-options.interface';
import type { CreateReplacementOptions } from './interfaces/create-replacement-options.interface';
import type { CreateRefundOptions } from './interfaces/create-refund-options.interface';
import type { RetryFulfillmentOptions } from './interfaces/retry-fulfillment-options.interface';
import type { RetryDynamicDeliveryOptions } from './interfaces/retry-dynamic-delivery-options.interface';
import type { SendFulfillmentNotificationsOptions } from './interfaces/send-fulfillment-notifications-options.interface';
import type { ListDeliverablesOptions } from './interfaces/list-deliverables-options.interface';
import type { CreateFromWalletOptions } from './interfaces/create-from-wallet-options.interface';
import type { PayFromWalletOptions } from './interfaces/pay-from-wallet-options.interface';
import type { ListOrdersResponseValue200ApplicationJsonPropertyDataItem, ListOrdersResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-orders-response-value-200-application-json-property-data-item.interface';
import type { SdkCreateOrderResponseValue201ApplicationJson, SdkCreateOrderResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-order-response-value-201-application-json.interface';
import type { SearchOrdersResponseValue200ApplicationJsonPropertyDataItem, SearchOrdersResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/search-orders-response-value-200-application-json-property-data-item.interface';
import type { SdkGetOrderResponseValue200ApplicationJson, SdkGetOrderResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-order-response-value-200-application-json.interface';
import type { SdkUpdateOrderStatusResponseValue200ApplicationJson, SdkUpdateOrderStatusResponseValue200ApplicationJsonResponse } from './interfaces/sdk-update-order-status-response-value-200-application-json.interface';
import type { SdkCreateOrderCheckoutResponseValue200ApplicationJson, SdkCreateOrderCheckoutResponseValue200ApplicationJsonResponse } from './interfaces/sdk-create-order-checkout-response-value-200-application-json.interface';
import type { SdkCreateOrderReplacementResponseValue200ApplicationJson, SdkCreateOrderReplacementResponseValue200ApplicationJsonResponse } from './interfaces/sdk-create-order-replacement-response-value-200-application-json.interface';
import type { SdkCreateOrderRefundResponseValue200ApplicationJson, SdkCreateOrderRefundResponseValue200ApplicationJsonResponse } from './interfaces/sdk-create-order-refund-response-value-200-application-json.interface';
import type { SdkRetryOrderFulfillmentResponseValue200ApplicationJson, SdkRetryOrderFulfillmentResponseValue200ApplicationJsonResponse } from './interfaces/sdk-retry-order-fulfillment-response-value-200-application-json.interface';
import type { SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson, SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJsonResponse } from './interfaces/sdk-retry-order-dynamic-delivery-response-value-202-application-json.interface';
import type { SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson, SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-send-order-fulfillment-notifications-response-value-200-application-json.interface';
import type { SdkListOrderDeliverablesResponseValue200ApplicationJson, SdkListOrderDeliverablesResponseValue200ApplicationJsonResponse } from './interfaces/sdk-list-order-deliverables-response-value-200-application-json.interface';
import type { SdkCreateWalletOrderResponseValue201ApplicationJson, SdkCreateWalletOrderResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-wallet-order-response-value-201-application-json.interface';
import type { SdkPayOrderFromWalletResponseValue200ApplicationJson, SdkPayOrderFromWalletResponseValue200ApplicationJsonResponse } from './interfaces/sdk-pay-order-from-wallet-response-value-200-application-json.interface';
import type { SdkCreateOrderRequestApplicationJsonResponse } from './interfaces/sdk-create-order-request-application-json.interface';
import type { SdkSearchOrdersRequestApplicationJsonResponse } from './interfaces/sdk-search-orders-request-application-json.interface';
import type { SdkUpdateOrderStatusRequestApplicationJsonResponse } from './interfaces/sdk-update-order-status-request-application-json.interface';
import type { SdkCreateOrderCheckoutRequestApplicationJsonResponse } from './interfaces/sdk-create-order-checkout-request-application-json.interface';
import type { SdkCreateOrderReplacementRequestApplicationJsonResponse } from './interfaces/sdk-create-order-replacement-request-application-json.interface';
import type { SdkCreateOrderRefundRequestApplicationJsonResponse } from './interfaces/sdk-create-order-refund-request-application-json.interface';
import type { SdkRetryOrderFulfillmentRequestApplicationJsonResponse } from './interfaces/sdk-retry-order-fulfillment-request-application-json.interface';
import type { SdkRetryOrderDynamicDeliveryRequestApplicationJsonResponse } from './interfaces/sdk-retry-order-dynamic-delivery-request-application-json.interface';
import type { SdkSendOrderFulfillmentNotificationsRequestApplicationJsonResponse } from './interfaces/sdk-send-order-fulfillment-notifications-request-application-json.interface';
import type { SdkCreateWalletOrderRequestApplicationJsonResponse } from './interfaces/sdk-create-wallet-order-request-application-json.interface';
import type { SdkPayOrderFromWalletRequestApplicationJsonResponse } from './interfaces/sdk-pay-order-from-wallet-request-application-json.interface';
import { deserializeListOrdersResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-orders-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkCreateOrderResponseValue201ApplicationJson } from './serializers/sdk-create-order-response-value-201-application-json.serializer';
import { deserializeSearchOrdersResponseValue200ApplicationJsonPropertyDataItem } from './serializers/search-orders-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkGetOrderResponseValue200ApplicationJson } from './serializers/sdk-get-order-response-value-200-application-json.serializer';
import { deserializeSdkUpdateOrderStatusResponseValue200ApplicationJson } from './serializers/sdk-update-order-status-response-value-200-application-json.serializer';
import { deserializeSdkCreateOrderCheckoutResponseValue200ApplicationJson } from './serializers/sdk-create-order-checkout-response-value-200-application-json.serializer';
import { deserializeSdkCreateOrderReplacementResponseValue200ApplicationJson } from './serializers/sdk-create-order-replacement-response-value-200-application-json.serializer';
import { deserializeSdkCreateOrderRefundResponseValue200ApplicationJson } from './serializers/sdk-create-order-refund-response-value-200-application-json.serializer';
import { deserializeSdkRetryOrderFulfillmentResponseValue200ApplicationJson } from './serializers/sdk-retry-order-fulfillment-response-value-200-application-json.serializer';
import { deserializeSdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson } from './serializers/sdk-retry-order-dynamic-delivery-response-value-202-application-json.serializer';
import { deserializeSdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson } from './serializers/sdk-send-order-fulfillment-notifications-response-value-200-application-json.serializer';
import { deserializeSdkListOrderDeliverablesResponseValue200ApplicationJson } from './serializers/sdk-list-order-deliverables-response-value-200-application-json.serializer';
import { deserializeSdkCreateWalletOrderResponseValue201ApplicationJson } from './serializers/sdk-create-wallet-order-response-value-201-application-json.serializer';
import { deserializeSdkPayOrderFromWalletResponseValue200ApplicationJson } from './serializers/sdk-pay-order-from-wallet-response-value-200-application-json.serializer';
import { serializeSdkCreateOrderRequestApplicationJson } from './serializers/sdk-create-order-request-application-json.serializer';
import { serializeSdkSearchOrdersRequestApplicationJson } from './serializers/sdk-search-orders-request-application-json.serializer';
import { serializeSdkUpdateOrderStatusRequestApplicationJson } from './serializers/sdk-update-order-status-request-application-json.serializer';
import { serializeSdkCreateOrderCheckoutRequestApplicationJson } from './serializers/sdk-create-order-checkout-request-application-json.serializer';
import { serializeSdkCreateOrderReplacementRequestApplicationJson } from './serializers/sdk-create-order-replacement-request-application-json.serializer';
import { serializeSdkCreateOrderRefundRequestApplicationJson } from './serializers/sdk-create-order-refund-request-application-json.serializer';
import { serializeSdkRetryOrderFulfillmentRequestApplicationJson } from './serializers/sdk-retry-order-fulfillment-request-application-json.serializer';
import { serializeSdkRetryOrderDynamicDeliveryRequestApplicationJson } from './serializers/sdk-retry-order-dynamic-delivery-request-application-json.serializer';
import { serializeSdkSendOrderFulfillmentNotificationsRequestApplicationJson } from './serializers/sdk-send-order-fulfillment-notifications-request-application-json.serializer';
import { serializeSdkCreateWalletOrderRequestApplicationJson } from './serializers/sdk-create-wallet-order-request-application-json.serializer';
import { serializeSdkPayOrderFromWalletRequestApplicationJson } from './serializers/sdk-pay-order-from-wallet-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class Orders {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List orders
   *
   * List orders for the selected store. Requires the invoice token ability. Results include customer and payment-provider summaries without sensitive data, totals in integer minor units, status timelines, and product-level line-item summaries from the same store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListOrdersResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: OrdersListOptions): Promise<AutoPaginatable<ListOrdersResponseValue200ApplicationJsonPropertyDataItem, OrdersListOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListOrdersResponseValue200ApplicationJsonPropertyDataItemResponse, ListOrdersResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/orders',
        deserializeListOrdersResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListOrdersResponseValue200ApplicationJsonPropertyDataItemResponse, ListOrdersResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/orders',
          deserializeListOrdersResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create an order
   *
   * Create an order with one or more product-variant line items and start checkout. This can create provider payment sessions, reserve stock or wallet funds, and begin fulfillment for a free or fully wallet-funded order. Requires the invoice ability or orders:write OAuth scope and current store permission. Use the same Idempotency-Key and identical body after a lost response; inspect the order before creating a new key. The payment.checkout_url is a handoff, not proof of payment. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkCreateOrderResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateOrderResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateOrderResponseValue201ApplicationJsonResponse, SdkCreateOrderRequestApplicationJsonResponse>(
      '/v2/orders',
      serializeSdkCreateOrderRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createOrder' },
    );
    return deserializeSdkCreateOrderResponseValue201ApplicationJson(data);
  }

  /**
   * Search orders
   *
   * Find orders using the same filters as the list endpoint. Requires the invoice token ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SearchOrdersResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchOrdersResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchOrdersResponseValue200ApplicationJsonResponse, SdkSearchOrdersRequestApplicationJsonResponse>(
      '/v2/orders/search',
      serializeSdkSearchOrdersRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchOrders' },
    );
    return deserializeSdkSearchOrdersResponseValue200ApplicationJson(data);
  }

  /**
   * Retrieve an order
   *
   * Retrieve an order and its product-level line-item summaries from the same store. Requires the invoice token ability. An ID from another store returns not found. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order path parameter.
   * @example 1042
   * @returns {Promise<SdkGetOrderResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetOrderResponseValue200ApplicationJson> {
    const { order } = options;
    const { data } = await this.sellApp.get<SdkGetOrderResponseValue200ApplicationJsonResponse>(`/v2/orders/${encodeURIComponent(order)}`, { ...options.requestOptions, operationId: 'getOrder' });
    return deserializeSdkGetOrderResponseValue200ApplicationJson(data);
  }

  /**
   * Update order status
   *
   * Change order lifecycle state. COMPLETED finalizes fulfillment of an already paid/partial/disputing order; it does not capture a pending payment. VOIDED can schedule provider side effects. REVIEW and DISPUTING have state restrictions. Send expected_status to prevent overwriting a changed state; an invalid transition, stale status, or busy lifecycle lock returns 422. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkUpdateOrderStatusResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async updateStatus(options: UpdateStatusOptions): Promise<SdkUpdateOrderStatusResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateOrderStatusResponseValue200ApplicationJsonResponse, SdkUpdateOrderStatusRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/status`,
      serializeSdkUpdateOrderStatusRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateOrderStatus' },
    );
    return deserializeSdkUpdateOrderStatusResponseValue200ApplicationJson(data);
  }

  /**
   * Create order checkout
   *
   * Start or reuse checkout for a PENDING order. A new nonfree checkout returns 201; an existing payment session or free order returns 200. Free or fully wallet-funded checkout can mark the order paid. Failures can mark the order FAILED and release wallet reservations. Do not infer payment from a checkout URL or browser return. The optional expected_status field is validated but is not a checkout concurrency precondition. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkCreateOrderCheckoutResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async createCheckout(options: CreateCheckoutOptions): Promise<SdkCreateOrderCheckoutResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateOrderCheckoutResponseValue200ApplicationJsonResponse, SdkCreateOrderCheckoutRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/checkout`,
      serializeSdkCreateOrderCheckoutRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createOrderCheckout' },
    );
    return deserializeSdkCreateOrderCheckoutResponseValue200ApplicationJson(data);
  }

  /**
   * Issue replacements
   *
   * Create a replacement order and begin fulfillment for selected purchased variants. Supply a variant ID, an array of variant IDs, or a map of variant IDs to replacement quantities (null means original quantity). Variants must belong to this order; replacement quantities cannot exceed purchased quantity or available stock. Already replaced deliveries cannot be replaced again. The returned ID belongs to the new replacement order. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkCreateOrderReplacementResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async createReplacement(options: CreateReplacementOptions): Promise<SdkCreateOrderReplacementResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateOrderReplacementResponseValue200ApplicationJsonResponse, SdkCreateOrderReplacementRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/replacements`,
      serializeSdkCreateOrderReplacementRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createOrderReplacement' },
    );
    return deserializeSdkCreateOrderReplacementResponseValue200ApplicationJson(data);
  }

  /**
   * Refund an order
   *
   * Request a real refund for a completed, nonsubscription order with refundable balance and a supported provider. amount is a decimal string in the order currency: "5.00" means $5.00 in USD, not 500 cents. Omitting amount, sending null, or an empty string requests the FULL remaining balance. Do not send amount_cents: it is not a request field. Inspect refund.status (pending/effective/failed/cancelled); a 200 response does not guarantee money has reached the customer. Retrieve refund state before retrying an uncertain provider outcome. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkCreateOrderRefundResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async createRefund(options: CreateRefundOptions): Promise<SdkCreateOrderRefundResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateOrderRefundResponseValue200ApplicationJsonResponse, SdkCreateOrderRefundRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/refunds`,
      serializeSdkCreateOrderRefundRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createOrderRefund' },
    );
    return deserializeSdkCreateOrderRefundResponseValue200ApplicationJson(data);
  }

  /**
   * Retry fulfillment
   *
   * Reset fulfillment effects for a PAID or PARTIAL order and retry delivery. The optional email overrides the recipient. meta.effects_reset counts reset effects; it does not prove fulfillment finished. Other order states and a busy lifecycle lock return 422. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkRetryOrderFulfillmentResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async retryFulfillment(options: RetryFulfillmentOptions): Promise<SdkRetryOrderFulfillmentResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkRetryOrderFulfillmentResponseValue200ApplicationJsonResponse, SdkRetryOrderFulfillmentRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/fulfillment-retries`,
      serializeSdkRetryOrderFulfillmentRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'retryOrderFulfillment' },
    );
    return deserializeSdkRetryOrderFulfillmentResponseValue200ApplicationJson(data);
  }

  /**
   * Retry dynamic delivery
   *
   * Queue another dynamic delivery attempt for one delivered product of a COMPLETED order. The delivered product, line item and currently dynamic variant must all belong to this order and store, with a configured webhook. 202 and meta.queued acknowledge the retry request, not successful external delivery. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async retryDynamicDelivery(options: RetryDynamicDeliveryOptions): Promise<SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkRetryOrderDynamicDeliveryResponseValue202ApplicationJsonResponse, SdkRetryOrderDynamicDeliveryRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/dynamic-delivery-retries`,
      serializeSdkRetryOrderDynamicDeliveryRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'retryOrderDynamicDelivery' },
    );
    return deserializeSdkRetryOrderDynamicDeliveryResponseValue202ApplicationJson(data);
  }

  /**
   * Send fulfillment notifications
   *
   * Send delivery notifications for each selected purchased variant. Omit product_variant_ids to select all variants. A supplied list must contain distinct positive IDs belonging to this order and store. Noncompleted orders or a missing recipient return meta.notifications_sent=0. The count records notifications requested, not email delivery confirmation. Requires the invoice ability or orders:write OAuth scope and current store permission. Idempotency-Key is required. Reuse the same key and identical request after a lost response; changed input or an in-progress request returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async sendFulfillmentNotifications(options: SendFulfillmentNotificationsOptions): Promise<SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJsonResponse, SdkSendOrderFulfillmentNotificationsRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/fulfillment-notifications`,
      serializeSdkSendOrderFulfillmentNotificationsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'sendOrderFulfillmentNotifications' },
    );
    return deserializeSdkSendOrderFulfillmentNotificationsResponseValue200ApplicationJson(data);
  }

  /**
   * List order deliverables
   *
   * List seller-visible fulfillment records, expanding purchased bundle snapshots into their component variants. File download links expire after one hour. This payload includes line-item and variant IDs but does not expose the delivered_product_id required for dynamic delivery retries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The order identifier.
   * @example 9001
   * @returns {Promise<SdkListOrderDeliverablesResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async listDeliverables(options: ListDeliverablesOptions): Promise<SdkListOrderDeliverablesResponseValue200ApplicationJson> {
    const { order } = options;
    const { data } = await this.sellApp.get<SdkListOrderDeliverablesResponseValue200ApplicationJsonResponse>(`/v2/orders/${encodeURIComponent(order)}/deliverables`, { ...options.requestOptions, operationId: 'listOrderDeliverables' });
    return deserializeSdkListOrderDeliverablesResponseValue200ApplicationJson(data);
  }

  /**
   * Create and pay an order from a wallet
   *
   * Create an order and debit the customer's wallet in one atomic call. This spends real wallet funds and queues normal fulfillment; PAID means payment was captured, while COMPLETED depends on fulfillment. Requires both invoice and wallet:write API-key abilities and both store permissions. Wallets must be enabled, the customer must already exist in this store, and an active wallet must cover the entire positive total after discounts and taxes. Subscriptions and partial wallet payments are not supported. Omit payment_method and custom_payment_method_id. An insufficient balance or invalid cart returns 422 without creating an order or debit. Send the required Idempotency-Key; retry an identical request with the same key after a lost response. Reusing it with different input returns 409. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkCreateWalletOrderResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async createFromWallet(options: CreateFromWalletOptions): Promise<SdkCreateWalletOrderResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateWalletOrderResponseValue201ApplicationJsonResponse, SdkCreateWalletOrderRequestApplicationJsonResponse>(
      '/v2/orders/wallet',
      serializeSdkCreateWalletOrderRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createWalletOrder' },
    );
    return deserializeSdkCreateWalletOrderResponseValue201ApplicationJson(data);
  }

  /**
   * Pay an existing order from its customer wallet
   *
   * Debit the order's customer wallet for the complete stored order total and queue normal fulfillment. Requires both invoice and wallet:write API-key abilities and both store permissions. Only PENDING orders with pending lines, no subscription, and no existing payment method or checkout session are eligible. Active provider checkouts cannot be replaced. The store must enable wallets and the customer's active wallet must cover the full amount. The order ID determines the customer and amount; neither can be overridden. Send the required Idempotency-Key and reuse it with identical input after a lost response. A changed expected_status returns 409; an ineligible order or insufficient balance returns 422 without a debit. PAID does not guarantee fulfillment has completed. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @example 42
   * @returns {Promise<SdkPayOrderFromWalletResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async payFromWallet(options: PayFromWalletOptions): Promise<SdkPayOrderFromWalletResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkPayOrderFromWalletResponseValue200ApplicationJsonResponse, SdkPayOrderFromWalletRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/wallet-payments`,
      serializeSdkPayOrderFromWalletRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'payOrderFromWallet' },
    );
    return deserializeSdkPayOrderFromWalletResponseValue200ApplicationJson(data);
  }
}
