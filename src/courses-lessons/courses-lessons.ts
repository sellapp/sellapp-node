// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { ReorderOptions } from './interfaces/reorder-options.interface';
import type { SdkCreateCourseLessonResponseValue201ApplicationJson, SdkCreateCourseLessonResponseValue201ApplicationJsonResponse } from '../courses/interfaces/sdk-create-course-lesson-response-value-201-application-json.interface';
import type { SdkReplaceCourseLessonResponseValue200ApplicationJson, SdkReplaceCourseLessonResponseValue200ApplicationJsonResponse } from '../courses/interfaces/sdk-replace-course-lesson-response-value-200-application-json.interface';
import type { SdkUpdateCourseLessonResponseValue200ApplicationJson, SdkUpdateCourseLessonResponseValue200ApplicationJsonResponse } from '../courses/interfaces/sdk-update-course-lesson-response-value-200-application-json.interface';
import type { SdkReorderCourseLessonsResponseValue200ApplicationJson, SdkReorderCourseLessonsResponseValue200ApplicationJsonResponse } from '../courses/interfaces/sdk-reorder-course-lessons-response-value-200-application-json.interface';
import type { SdkCreateCourseLessonRequestApplicationJsonResponse } from '../courses/interfaces/sdk-create-course-lesson-request-application-json.interface';
import type { SdkReplaceCourseLessonRequestApplicationJsonResponse } from '../courses/interfaces/sdk-replace-course-lesson-request-application-json.interface';
import type { SdkUpdateCourseLessonRequestApplicationJsonResponse } from '../courses/interfaces/sdk-update-course-lesson-request-application-json.interface';
import type { SdkReorderCourseLessonsRequestApplicationJsonResponse } from '../courses/interfaces/sdk-reorder-course-lessons-request-application-json.interface';
import { deserializeSdkCreateCourseLessonResponseValue201ApplicationJson } from '../courses/serializers/sdk-create-course-lesson-response-value-201-application-json.serializer';
import { deserializeSdkReplaceCourseLessonResponseValue200ApplicationJson } from '../courses/serializers/sdk-replace-course-lesson-response-value-200-application-json.serializer';
import { deserializeSdkUpdateCourseLessonResponseValue200ApplicationJson } from '../courses/serializers/sdk-update-course-lesson-response-value-200-application-json.serializer';
import { deserializeSdkReorderCourseLessonsResponseValue200ApplicationJson } from '../courses/serializers/sdk-reorder-course-lessons-response-value-200-application-json.serializer';
import { serializeSdkCreateCourseLessonRequestApplicationJson } from '../courses/serializers/sdk-create-course-lesson-request-application-json.serializer';
import { serializeSdkReplaceCourseLessonRequestApplicationJson } from '../courses/serializers/sdk-replace-course-lesson-request-application-json.serializer';
import { serializeSdkUpdateCourseLessonRequestApplicationJson } from '../courses/serializers/sdk-update-course-lesson-request-application-json.serializer';
import { serializeSdkReorderCourseLessonsRequestApplicationJson } from '../courses/serializers/sdk-reorder-course-lessons-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class CoursesLessons {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * Create a course lesson
   *
   * Append a lesson, quiz, or assignment to a section. Rich text is sanitized before storage. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.section - The section path parameter.
   * @returns {Promise<SdkCreateCourseLessonResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateCourseLessonResponseValue201ApplicationJson> {
    const { course, section, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateCourseLessonResponseValue201ApplicationJsonResponse, SdkCreateCourseLessonRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/sections/${encodeURIComponent(section)}/lessons`,
      serializeSdkCreateCourseLessonRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createCourseLesson' },
    );
    return deserializeSdkCreateCourseLessonResponseValue201ApplicationJson(data);
  }

  /**
   * Update a course lesson
   *
   * Update lesson content, draft state, preview state, or replace the structured quiz or assignment body. Item type is immutable except while choosing video or text for a new lecture placeholder. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.lesson - The lesson path parameter.
   * @returns {Promise<SdkReplaceCourseLessonResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceCourseLessonResponseValue200ApplicationJson> {
    const { course, lesson, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceCourseLessonResponseValue200ApplicationJsonResponse, SdkReplaceCourseLessonRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/lessons/${encodeURIComponent(lesson)}`,
      serializeSdkReplaceCourseLessonRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceCourseLesson' },
    );
    return deserializeSdkReplaceCourseLessonResponseValue200ApplicationJson(data);
  }

  /**
   * Update a course lesson
   *
   * Update lesson content, draft state, preview state, or replace the structured quiz or assignment body. Item type is immutable except while choosing video or text for a new lecture placeholder. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.lesson - The lesson path parameter.
   * @returns {Promise<SdkUpdateCourseLessonResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateCourseLessonResponseValue200ApplicationJson> {
    const { course, lesson, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateCourseLessonResponseValue200ApplicationJsonResponse, SdkUpdateCourseLessonRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/lessons/${encodeURIComponent(lesson)}`,
      serializeSdkUpdateCourseLessonRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateCourseLesson' },
    );
    return deserializeSdkUpdateCourseLessonResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a course lesson
   *
   * Delete a lesson and its structured questions, assignment, attachments, and video reference, then normalize lesson order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @param options.lesson - The lesson path parameter.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { course, lesson } = options;
    await this.sellApp.delete(`/v2/courses/${encodeURIComponent(course)}/lessons/${encodeURIComponent(lesson)}`, { ...options.requestOptions, operationId: 'deleteCourseLesson' });
  }

  /**
   * Reorder course lessons
   *
   * Replace the complete lesson order and section placement. Send every lesson ID exactly once with a destination section from the same course. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.course - The course path parameter.
   * @returns {Promise<SdkReorderCourseLessonsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async reorder(options: ReorderOptions): Promise<SdkReorderCourseLessonsResponseValue200ApplicationJson> {
    const { course, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkReorderCourseLessonsResponseValue200ApplicationJsonResponse, SdkReorderCourseLessonsRequestApplicationJsonResponse>(
      `/v2/courses/${encodeURIComponent(course)}/lessons/reorder`,
      serializeSdkReorderCourseLessonsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'reorderCourseLessons' },
    );
    return deserializeSdkReorderCourseLessonsResponseValue200ApplicationJson(data);
  }
}
