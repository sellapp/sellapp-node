// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { ListWebhookDeliveriesOptions } from './interfaces/list-webhook-deliveries-options.interface';
import type { GetWebhookDeliveryOptions } from './interfaces/get-webhook-delivery-options.interface';
import type { ReplayWebhookDeliveryOptions } from './interfaces/replay-webhook-delivery-options.interface';
import type { SdkListWebhookEventTypesResponseValue200ApplicationJson, SdkListWebhookEventTypesResponseValue200ApplicationJsonResponse } from './interfaces/sdk-list-webhook-event-types-response-value-200-application-json.interface';
import type { ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem, ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-webhook-deliveries-response-value-200-application-json-property-data-item.interface';
import type { SdkGetWebhookDeliveryResponseValue200ApplicationJson, SdkGetWebhookDeliveryResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-webhook-delivery-response-value-200-application-json.interface';
import type { SdkReplayWebhookDeliveryResponseValue201ApplicationJson, SdkReplayWebhookDeliveryResponseValue201ApplicationJsonResponse } from './interfaces/sdk-replay-webhook-delivery-response-value-201-application-json.interface';
import type { SdkReplayWebhookDeliveryRequestApplicationJsonResponse } from './interfaces/sdk-replay-webhook-delivery-request-application-json.interface';
import { deserializeSdkListWebhookEventTypesResponseValue200ApplicationJson } from './serializers/sdk-list-webhook-event-types-response-value-200-application-json.serializer';
import { deserializeListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-webhook-deliveries-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkGetWebhookDeliveryResponseValue200ApplicationJson } from './serializers/sdk-get-webhook-delivery-response-value-200-application-json.serializer';
import { deserializeSdkReplayWebhookDeliveryResponseValue201ApplicationJson } from './serializers/sdk-replay-webhook-delivery-response-value-201-application-json.serializer';
import { serializeSdkReplayWebhookDeliveryRequestApplicationJson } from './serializers/sdk-replay-webhook-delivery-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class WebhookPlatform {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List webhook event types
   *
   * List versioned outbound event types. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkListWebhookEventTypesResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async listWebhookEventTypes(requestOptions: RequestOptions = {}): Promise<SdkListWebhookEventTypesResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkListWebhookEventTypesResponseValue200ApplicationJsonResponse>('/v2/webhook-event-types', requestOptions);
    return deserializeSdkListWebhookEventTypesResponseValue200ApplicationJson(data);
  }

  /**
   * List webhook deliveries
   *
   * Destination origin only; response bodies are redacted and capped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async listWebhookDeliveries(options?: ListWebhookDeliveriesOptions): Promise<AutoPaginatable<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem, ListWebhookDeliveriesOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItemResponse, ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/webhook-deliveries',
        deserializeListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItemResponse, ListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/webhook-deliveries',
          deserializeListWebhookDeliveriesResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Retrieve a webhook delivery
   *
   * Retrieve one redacted delivery. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.delivery - The delivery identifier.
   * @example "01992b31-c8bd-75b5-b02d-6ae0aa418940"
   * @returns {Promise<SdkGetWebhookDeliveryResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async getWebhookDelivery(options: GetWebhookDeliveryOptions): Promise<SdkGetWebhookDeliveryResponseValue200ApplicationJson> {
    const { delivery } = options;
    const { data } = await this.sellApp.get<SdkGetWebhookDeliveryResponseValue200ApplicationJsonResponse>(`/v2/webhook-deliveries/${encodeURIComponent(delivery)}`, { ...options.requestOptions, operationId: 'getWebhookDelivery' });
    return deserializeSdkGetWebhookDeliveryResponseValue200ApplicationJson(data);
  }

  /**
   * Replay a webhook delivery
   *
   * Accept no URL, payload, or query overrides. Resolve the current channel server-side, create a new delivery ID, retain the integration-event ID, and sign with a new timestamp. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.delivery - The delivery identifier.
   * @example "delivery_01K4CUSTOMER"
   * @returns {Promise<SdkReplayWebhookDeliveryResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replayWebhookDelivery(options: ReplayWebhookDeliveryOptions): Promise<SdkReplayWebhookDeliveryResponseValue201ApplicationJson> {
    const { delivery, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkReplayWebhookDeliveryResponseValue201ApplicationJsonResponse, SdkReplayWebhookDeliveryRequestApplicationJsonResponse>(
      `/v2/webhook-deliveries/${encodeURIComponent(delivery)}/replay`,
      serializeSdkReplayWebhookDeliveryRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replayWebhookDelivery' },
    );
    return deserializeSdkReplayWebhookDeliveryResponseValue201ApplicationJson(data);
  }
}
