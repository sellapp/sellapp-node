// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { OrdersLineItemsListOptions } from './interfaces/orders-line-items-list-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem, ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../line-items/interfaces/list-order-s-line-items-response-value-200-application-json-property-data-item.interface';
import type { SearchOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem, SearchOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../line-items/interfaces/search-order-s-line-items-response-value-200-application-json-property-data-item.interface';
import type { SdkGetOrderSLineItemResponseValue200ApplicationJson, SdkGetOrderSLineItemResponseValue200ApplicationJsonResponse } from '../line-items/interfaces/sdk-get-order-s-line-item-response-value-200-application-json.interface';
import type { SdkSearchOrderSLineItemsRequestApplicationJsonResponse } from '../line-items/interfaces/sdk-search-order-s-line-items-request-application-json.interface';
import { deserializeListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem } from '../line-items/serializers/list-order-s-line-items-response-value-200-application-json-property-data-item.serializer';
import { deserializeSearchOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem } from '../line-items/serializers/search-order-s-line-items-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkGetOrderSLineItemResponseValue200ApplicationJson } from '../line-items/serializers/sdk-get-order-s-line-item-response-value-200-application-json.serializer';
import { serializeSdkSearchOrderSLineItemsRequestApplicationJson } from '../line-items/serializers/sdk-search-order-s-line-items-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class OrdersLineItems {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List an order's line items
   *
   * List purchase line items for the selected store. Requires the `invoice` API-key ability or the corresponding OAuth scope and current store permission. The order path parameter scopes the result and must match the line item's parent order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @param options.order - The parent order ID.
   * @example 4001
   * @returns {Promise<AutoPaginatable<ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options: OrdersLineItemsListOptions): Promise<AutoPaginatable<ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem, Omit<OrdersLineItemsListOptions, 'order'>>> {
    const { order, ...paginationOptions } = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItemResponse, ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        `/v2/orders/${encodeURIComponent(order)}/line-items`,
        deserializeListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItemResponse, ListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          `/v2/orders/${encodeURIComponent(order)}/line-items`,
          deserializeListOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Search an order's line items
   *
   * Search product-level sale records with filters and sorting. Requires the `invoice` token ability. The order path parameter scopes the result and must match the line item's parent order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @param options.order - The parent order ID.
   * @example 4001
   * @returns {Promise<AutoPaginatable<SearchOrderSLineItemsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchOrderSLineItemsResponseValue200ApplicationJson> {
    const { order, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchOrderSLineItemsResponseValue200ApplicationJsonResponse, SdkSearchOrderSLineItemsRequestApplicationJsonResponse>(
      `/v2/orders/${encodeURIComponent(order)}/line-items/search`,
      serializeSdkSearchOrderSLineItemsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchOrderSLineItems' },
    );
    return deserializeSdkSearchOrderSLineItemsResponseValue200ApplicationJson(data);
  }

  /**
   * Retrieve an order's line item
   *
   * Retrieve one product-level sale record for the selected store. Requires the `invoice` token ability. Records from another store or whose order does not match the store return `404`. The order path parameter scopes the result and must match the line item's parent order. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.order - The parent order ID.
   * @example 4001
   * @param options.lineItem - The purchase line-item ID.
   * @example 9001
   * @returns {Promise<SdkGetOrderSLineItemResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetOrderSLineItemResponseValue200ApplicationJson> {
    const { order, lineItem } = options;
    const { data } = await this.sellApp.get<SdkGetOrderSLineItemResponseValue200ApplicationJsonResponse>(`/v2/orders/${encodeURIComponent(order)}/line-items/${encodeURIComponent(lineItem)}`, { ...options.requestOptions, operationId: 'getOrderSLineItem' });
    return deserializeSdkGetOrderSLineItemResponseValue200ApplicationJson(data);
  }
}
