// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { BlacklistsListOptions } from './interfaces/blacklists-list-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { V2ListBlacklistsOptions } from './interfaces/v-2-list-blacklists-options.interface';
import type { V2CreateBlacklistOptions } from './interfaces/v-2-create-blacklist-options.interface';
import type { V2GetBlacklistOptions } from './interfaces/v-2-get-blacklist-options.interface';
import type { V2ReplaceBlacklistOptions } from './interfaces/v-2-replace-blacklist-options.interface';
import type { V2UpdateBlacklistOptions } from './interfaces/v-2-update-blacklist-options.interface';
import type { V2DeleteBlacklistOptions } from './interfaces/v-2-delete-blacklist-options.interface';
import type { Blacklist, BlacklistResponse as _OagenBlacklistWire } from './interfaces/blacklist.interface';
import type { BlacklistResponse, BlacklistResponseWire } from './interfaces/blacklist-response.interface';
import type { BlacklistCreateRequestResponse } from './interfaces/blacklist-create-request.interface';
import type { BlacklistUpdateRequestResponse } from './interfaces/blacklist-update-request.interface';
import { deserializeBlacklist } from './serializers/blacklist.serializer';
import { deserializeBlacklistResponse } from './serializers/blacklist-response.serializer';
import { serializeBlacklistCreateRequest } from './serializers/blacklist-create-request.serializer';
import { serializeBlacklistUpdateRequest } from './serializers/blacklist-update-request.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class Blacklists {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List all blacklist rules
   *
   * Retrieve a paginated list of blacklist rules for your store. By default, a maximum of fifteen blacklist rules are returned per page.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<Blacklist>>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: BlacklistsListOptions): Promise<AutoPaginatable<Blacklist, BlacklistsListOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<_OagenBlacklistWire, Blacklist>(
        this.sellApp,
        '/v1/blacklists',
        deserializeBlacklist,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<_OagenBlacklistWire, Blacklist>(
          this.sellApp,
          '/v1/blacklists',
          deserializeBlacklist,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create a blacklist rule
   *
   * Create a new blacklist rule for your store.
   * @returns {Promise<BlacklistResponse>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<BlacklistResponse> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<BlacklistResponseWire, BlacklistCreateRequestResponse>(
      '/v1/blacklists',
      serializeBlacklistCreateRequest(payload), { ...options.requestOptions, operationId: 'createBlacklist' },
    );
    return deserializeBlacklistResponse(data);
  }

  /**
   * Retrieve a blacklist rule
   *
   * Retrieve a specific blacklist rule by its unique identifier.
   * @param options - The request options.
   * @param options.blacklist - The unique identifier of the blacklist rule.
   * @example 1
   * @returns {Promise<BlacklistResponse>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<BlacklistResponse> {
    const { blacklist } = options;
    const { data } = await this.sellApp.get<BlacklistResponseWire>(`/v1/blacklists/${encodeURIComponent(blacklist)}`, { ...options.requestOptions, operationId: 'getBlacklist' });
    return deserializeBlacklistResponse(data);
  }

  /**
   * Update a blacklist rule
   *
   * Update one or more attributes on an existing blacklist rule.
   * @param options - The request options.
   * @param options.blacklist - The unique identifier of the blacklist rule.
   * @example 1
   * @returns {Promise<BlacklistResponse>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<BlacklistResponse> {
    const { blacklist, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<BlacklistResponseWire, BlacklistUpdateRequestResponse>(
      `/v1/blacklists/${encodeURIComponent(blacklist)}`,
      serializeBlacklistUpdateRequest(payload), { ...options.requestOptions, operationId: 'updateBlacklist' },
    );
    return deserializeBlacklistResponse(data);
  }

  /**
   * Delete a blacklist rule
   *
   * Permanently delete a blacklist rule from your store.
   * @param options - The request options.
   * @param options.blacklist - The unique identifier of the blacklist rule.
   * @example 1
   * @returns {Promise<void>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { blacklist } = options;
    await this.sellApp.delete(`/v1/blacklists/${encodeURIComponent(blacklist)}`, { ...options.requestOptions, operationId: 'deleteBlacklist' });
  }

  /**
   * List blacklist rules
   *
   * List store-scoped blacklist rules. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<Blacklist>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2ListBlacklists(options?: V2ListBlacklistsOptions): Promise<AutoPaginatable<Blacklist, V2ListBlacklistsOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<_OagenBlacklistWire, Blacklist>(
        this.sellApp,
        '/v2/blacklists',
        deserializeBlacklist,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<_OagenBlacklistWire, Blacklist>(
          this.sellApp,
          '/v2/blacklists',
          deserializeBlacklist,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create a blacklist rule
   *
   * Create a store-scoped blacklist rule. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<BlacklistResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2CreateBlacklist(options: V2CreateBlacklistOptions): Promise<BlacklistResponse> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<BlacklistResponseWire, BlacklistCreateRequestResponse>(
      '/v2/blacklists',
      serializeBlacklistCreateRequest(payload), { ...options.requestOptions, operationId: 'v2CreateBlacklist' },
    );
    return deserializeBlacklistResponse(data);
  }

  /**
   * Retrieve a blacklist rule
   *
   * Retrieve a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.blacklist - The blacklist identifier.
   * @example 42
   * @returns {Promise<BlacklistResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2GetBlacklist(options: V2GetBlacklistOptions): Promise<BlacklistResponse> {
    const { blacklist } = options;
    const { data } = await this.sellApp.get<BlacklistResponseWire>(`/v2/blacklists/${encodeURIComponent(blacklist)}`, { ...options.requestOptions, operationId: 'v2GetBlacklist' });
    return deserializeBlacklistResponse(data);
  }

  /**
   * Replace a blacklist rule
   *
   * Replace a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.blacklist - The blacklist identifier.
   * @example 42
   * @returns {Promise<BlacklistResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2ReplaceBlacklist(options: V2ReplaceBlacklistOptions): Promise<BlacklistResponse> {
    const { blacklist, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<BlacklistResponseWire, BlacklistUpdateRequestResponse>(
      `/v2/blacklists/${encodeURIComponent(blacklist)}`,
      serializeBlacklistUpdateRequest(payload), { ...options.requestOptions, operationId: 'v2ReplaceBlacklist' },
    );
    return deserializeBlacklistResponse(data);
  }

  /**
   * Update a blacklist rule
   *
   * Update a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.blacklist - The blacklist identifier.
   * @example 42
   * @returns {Promise<BlacklistResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2UpdateBlacklist(options: V2UpdateBlacklistOptions): Promise<BlacklistResponse> {
    const { blacklist, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<BlacklistResponseWire, BlacklistUpdateRequestResponse>(
      `/v2/blacklists/${encodeURIComponent(blacklist)}`,
      serializeBlacklistUpdateRequest(payload), { ...options.requestOptions, operationId: 'v2UpdateBlacklist' },
    );
    return deserializeBlacklistResponse(data);
  }

  /**
   * Delete a blacklist rule
   *
   * Delete a blacklist rule in the selected store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.blacklist - The blacklist identifier.
   * @example 42
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2DeleteBlacklist(options: V2DeleteBlacklistOptions): Promise<void> {
    const { blacklist } = options;
    await this.sellApp.delete(`/v2/blacklists/${encodeURIComponent(blacklist)}`, { ...options.requestOptions, operationId: 'v2DeleteBlacklist' });
  }
}
