// This file is auto-generated by oagen. Do not edit.

import type { GetOptions as RequestOptions } from '../../common/interfaces/get-options.interface';
import type { SdkUpdateCouponRequestApplicationJsonType } from '../../blacklists/interfaces/sdk-update-coupon-request-application-json-type.interface';

export interface UpdateOptions {
  /** Per-call timeout, retry, headers, cancellation, and lifecycle metadata. */
  requestOptions?: RequestOptions;
  /** The coupon path parameter. */
  coupon: number;
  /** Include soft-deleted resources in the result. */
  withTrashed?: boolean;
  /** Return only soft-deleted resources. Ignored when with_trashed is true. */
  onlyTrashed?: boolean;
  code?: string;
  type?: SdkUpdateCouponRequestApplicationJsonType | (string & Record<never, never>);
  /** A decimal discount value of at least 1. Percentage discounts cannot exceed 100; amount discounts use the store currency's major unit. */
  discount?: number | string;
  storeWide?: boolean;
  /** Product IDs the coupon applies to when store_wide is false. */
  products?: number[];
  /** Optional variant restrictions. Omit to preserve existing restrictions or send an empty array to allow every variant of the selected products. */
  productVariants?: number[];
  limit?: number | null;
  /** A future date and time, or null for no expiry. */
  expiresAt?: Date | null;
  /** A decimal minimum order amount in the store currency's major unit, or null for no minimum. */
  minimumAmount?: number | string | null;
}
