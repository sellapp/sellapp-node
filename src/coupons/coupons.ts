// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { PostOptions } from '../common/interfaces/post-options.interface';
import type { CouponsListOptions } from './interfaces/coupons-list-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { BatchCreateOptions } from './interfaces/batch-create-options.interface';
import type { BatchUpdateOptions } from './interfaces/batch-update-options.interface';
import type { BatchDeleteOptions } from './interfaces/batch-delete-options.interface';
import type { V2ListCouponsOptions } from './interfaces/v-2-list-coupons-options.interface';
import type { V2CreateCouponOptions } from './interfaces/v-2-create-coupon-options.interface';
import type { V2SearchCouponsOptions } from './interfaces/v-2-search-coupons-options.interface';
import type { V2GetCouponOptions } from './interfaces/v-2-get-coupon-options.interface';
import type { V2ReplaceCouponOptions } from './interfaces/v-2-replace-coupon-options.interface';
import type { V2UpdateCouponOptions } from './interfaces/v-2-update-coupon-options.interface';
import type { V2DeleteCouponOptions } from './interfaces/v-2-delete-coupon-options.interface';
import type { V2BatchCreateCouponsOptions } from './interfaces/v-2-batch-create-coupons-options.interface';
import type { V2BatchUpdateCouponsOptions } from './interfaces/v-2-batch-update-coupons-options.interface';
import type { V2BatchDeleteCouponsOptions } from './interfaces/v-2-batch-delete-coupons-options.interface';
import type { ListCouponsResponseValue200ApplicationJsonPropertyDataItem, ListCouponsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../blacklists/interfaces/list-coupons-response-value-200-application-json-property-data-item.interface';
import type { SdkCreateCouponResponseValue201ApplicationJson, SdkCreateCouponResponseValue201ApplicationJsonResponse } from '../blacklists/interfaces/sdk-create-coupon-response-value-201-application-json.interface';
import type { SdkGetCouponResponseValue200ApplicationJson, SdkGetCouponResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-get-coupon-response-value-200-application-json.interface';
import type { SdkReplaceCouponResponseValue200ApplicationJson, SdkReplaceCouponResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-replace-coupon-response-value-200-application-json.interface';
import type { SdkUpdateCouponResponseValue200ApplicationJson, SdkUpdateCouponResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-update-coupon-response-value-200-application-json.interface';
import type { SearchCouponsResponseValue200ApplicationJsonPropertyDataItem, SearchCouponsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../blacklists/interfaces/search-coupons-response-value-200-application-json-property-data-item.interface';
import type { SdkBatchCreateCouponsResponseValue200ApplicationJson, SdkBatchCreateCouponsResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-batch-create-coupons-response-value-200-application-json.interface';
import type { SdkBatchUpdateCouponsResponseValue200ApplicationJson, SdkBatchUpdateCouponsResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-batch-update-coupons-response-value-200-application-json.interface';
import type { V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem, V2ListCouponsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../blacklists/interfaces/v-2-list-coupons-response-value-200-application-json-property-data-item.interface';
import type { SdkV2CreateCouponResponseValue201ApplicationJson, SdkV2CreateCouponResponseValue201ApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-create-coupon-response-value-201-application-json.interface';
import type { V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem, V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../blacklists/interfaces/v-2-search-coupons-response-value-200-application-json-property-data-item.interface';
import type { SdkV2GetCouponResponseValue200ApplicationJson, SdkV2GetCouponResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-get-coupon-response-value-200-application-json.interface';
import type { SdkV2ReplaceCouponResponseValue200ApplicationJson, SdkV2ReplaceCouponResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-replace-coupon-response-value-200-application-json.interface';
import type { SdkV2UpdateCouponResponseValue200ApplicationJson, SdkV2UpdateCouponResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-update-coupon-response-value-200-application-json.interface';
import type { SdkV2BatchCreateCouponsResponseValue200ApplicationJson, SdkV2BatchCreateCouponsResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-batch-create-coupons-response-value-200-application-json.interface';
import type { SdkV2BatchUpdateCouponsResponseValue200ApplicationJson, SdkV2BatchUpdateCouponsResponseValue200ApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-batch-update-coupons-response-value-200-application-json.interface';
import type { SdkCreateCouponRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-create-coupon-request-application-json.interface';
import type { SdkReplaceCouponRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-replace-coupon-request-application-json.interface';
import type { SdkUpdateCouponRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-update-coupon-request-application-json.interface';
import type { SdkSearchCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-search-coupons-request-application-json.interface';
import type { SdkBatchCreateCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-batch-create-coupons-request-application-json.interface';
import type { SdkBatchUpdateCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-batch-update-coupons-request-application-json.interface';
import type { SdkBatchDeleteCouponsRequestApplicationJsonResponse } from '../product-variants/interfaces/sdk-batch-delete-coupons-request-application-json.interface';
import type { SdkV2CreateCouponRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-create-coupon-request-application-json.interface';
import type { SdkV2SearchCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-search-coupons-request-application-json.interface';
import type { SdkV2ReplaceCouponRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-replace-coupon-request-application-json.interface';
import type { SdkV2UpdateCouponRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-update-coupon-request-application-json.interface';
import type { SdkV2BatchCreateCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-batch-create-coupons-request-application-json.interface';
import type { SdkV2BatchUpdateCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-batch-update-coupons-request-application-json.interface';
import type { SdkV2BatchDeleteCouponsRequestApplicationJsonResponse } from '../blacklists/interfaces/sdk-v-2-batch-delete-coupons-request-application-json.interface';
import { deserializeListCouponsResponseValue200ApplicationJsonPropertyDataItem } from '../blacklists/serializers/list-coupons-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkCreateCouponResponseValue201ApplicationJson } from '../blacklists/serializers/sdk-create-coupon-response-value-201-application-json.serializer';
import { deserializeSdkGetCouponResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-get-coupon-response-value-200-application-json.serializer';
import { deserializeSdkReplaceCouponResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-replace-coupon-response-value-200-application-json.serializer';
import { deserializeSdkUpdateCouponResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-update-coupon-response-value-200-application-json.serializer';
import { deserializeSearchCouponsResponseValue200ApplicationJsonPropertyDataItem } from '../blacklists/serializers/search-coupons-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkBatchCreateCouponsResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-batch-create-coupons-response-value-200-application-json.serializer';
import { deserializeSdkBatchUpdateCouponsResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-batch-update-coupons-response-value-200-application-json.serializer';
import { deserializeV2ListCouponsResponseValue200ApplicationJsonPropertyDataItem } from '../blacklists/serializers/v-2-list-coupons-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkV2CreateCouponResponseValue201ApplicationJson } from '../blacklists/serializers/sdk-v-2-create-coupon-response-value-201-application-json.serializer';
import { deserializeV2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem } from '../blacklists/serializers/v-2-search-coupons-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkV2GetCouponResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-v-2-get-coupon-response-value-200-application-json.serializer';
import { deserializeSdkV2ReplaceCouponResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-v-2-replace-coupon-response-value-200-application-json.serializer';
import { deserializeSdkV2UpdateCouponResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-v-2-update-coupon-response-value-200-application-json.serializer';
import { deserializeSdkV2BatchCreateCouponsResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-v-2-batch-create-coupons-response-value-200-application-json.serializer';
import { deserializeSdkV2BatchUpdateCouponsResponseValue200ApplicationJson } from '../blacklists/serializers/sdk-v-2-batch-update-coupons-response-value-200-application-json.serializer';
import { serializeSdkCreateCouponRequestApplicationJson } from '../blacklists/serializers/sdk-create-coupon-request-application-json.serializer';
import { serializeSdkReplaceCouponRequestApplicationJson } from '../blacklists/serializers/sdk-replace-coupon-request-application-json.serializer';
import { serializeSdkUpdateCouponRequestApplicationJson } from '../blacklists/serializers/sdk-update-coupon-request-application-json.serializer';
import { serializeSdkSearchCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-search-coupons-request-application-json.serializer';
import { serializeSdkBatchCreateCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-batch-create-coupons-request-application-json.serializer';
import { serializeSdkBatchUpdateCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-batch-update-coupons-request-application-json.serializer';
import { serializeSdkBatchDeleteCouponsRequestApplicationJson } from '../product-variants/serializers/sdk-batch-delete-coupons-request-application-json.serializer';
import { serializeSdkV2CreateCouponRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-create-coupon-request-application-json.serializer';
import { serializeSdkV2SearchCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-search-coupons-request-application-json.serializer';
import { serializeSdkV2ReplaceCouponRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-replace-coupon-request-application-json.serializer';
import { serializeSdkV2UpdateCouponRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-update-coupon-request-application-json.serializer';
import { serializeSdkV2BatchCreateCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-batch-create-coupons-request-application-json.serializer';
import { serializeSdkV2BatchUpdateCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-batch-update-coupons-request-application-json.serializer';
import { serializeSdkV2BatchDeleteCouponsRequestApplicationJson } from '../blacklists/serializers/sdk-v-2-batch-delete-coupons-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

const serializeCouponsListOptions = (options: CouponsListOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.page !== undefined) wire.page = options.page;
  if (options.withTrashed !== undefined) wire.with_trashed = options.withTrashed;
  if (options.onlyTrashed !== undefined) wire.only_trashed = options.onlyTrashed;
  return wire as PaginationOptions;
};

const serializeSearchOptions = (options: SearchOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.withTrashed !== undefined) wire.with_trashed = options.withTrashed;
  if (options.onlyTrashed !== undefined) wire.only_trashed = options.onlyTrashed;
  return wire as PaginationOptions;
};

const serializeV2ListCouponsOptions = (options: V2ListCouponsOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.page !== undefined) wire.page = options.page;
  if (options.withTrashed !== undefined) wire.with_trashed = options.withTrashed;
  if (options.onlyTrashed !== undefined) wire.only_trashed = options.onlyTrashed;
  return wire as PaginationOptions;
};

const serializeV2SearchCouponsOptions = (options: V2SearchCouponsOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.withTrashed !== undefined) wire.with_trashed = options.withTrashed;
  if (options.onlyTrashed !== undefined) wire.only_trashed = options.onlyTrashed;
  return wire as PaginationOptions;
};

export class Coupons {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List all coupons
   *
   * List your store's coupons, 15 per page by default.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListCouponsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: CouponsListOptions): Promise<AutoPaginatable<ListCouponsResponseValue200ApplicationJsonPropertyDataItem, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<ListCouponsResponseValue200ApplicationJsonPropertyDataItemResponse, ListCouponsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v1/coupons',
        deserializeListCouponsResponseValue200ApplicationJsonPropertyDataItem,
        options ? serializeCouponsListOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<ListCouponsResponseValue200ApplicationJsonPropertyDataItemResponse, ListCouponsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v1/coupons',
          deserializeListCouponsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      options ? serializeCouponsListOptions(options) : undefined,
    );
  }

  /**
   * Create a coupon
   *
   * Create a discount code for your store.
   * @returns {Promise<SdkCreateCouponResponseValue201ApplicationJson>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateCouponResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateCouponResponseValue201ApplicationJsonResponse, SdkCreateCouponRequestApplicationJsonResponse>(
      '/v1/coupons',
      serializeSdkCreateCouponRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createCoupon' },
    );
    return deserializeSdkCreateCouponResponseValue201ApplicationJson(data);
  }

  /**
   * Retrieve a coupon
   *
   * Retrieve a coupon by its ID.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkGetCouponResponseValue200ApplicationJson>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetCouponResponseValue200ApplicationJson> {
    const { coupon } = options;
    const { data } = await this.sellApp.get<SdkGetCouponResponseValue200ApplicationJsonResponse>(`/v1/coupons/${encodeURIComponent(coupon)}`, { ...options.requestOptions, operationId: 'getCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } });
    return deserializeSdkGetCouponResponseValue200ApplicationJson(data);
  }

  /**
   * Update a coupon
   *
   * Change a coupon's discount, limits, or eligible products.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkReplaceCouponResponseValue200ApplicationJson>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceCouponResponseValue200ApplicationJson> {
    const { coupon, withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceCouponResponseValue200ApplicationJsonResponse, SdkReplaceCouponRequestApplicationJsonResponse>(
      `/v1/coupons/${encodeURIComponent(coupon)}`,
      serializeSdkReplaceCouponRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkReplaceCouponResponseValue200ApplicationJson(data);
  }

  /**
   * Update a coupon
   *
   * Change a coupon's discount, limits, or eligible products.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkUpdateCouponResponseValue200ApplicationJson>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateCouponResponseValue200ApplicationJson> {
    const { coupon, withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateCouponResponseValue200ApplicationJsonResponse, SdkUpdateCouponRequestApplicationJsonResponse>(
      `/v1/coupons/${encodeURIComponent(coupon)}`,
      serializeSdkUpdateCouponRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkUpdateCouponResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a coupon
   *
   * Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed or only_trashed to retrieve soft-deleted coupons later.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<void>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { coupon } = options;
    await this.sellApp.delete(`/v1/coupons/${encodeURIComponent(coupon)}`, { ...options.requestOptions, operationId: 'deleteCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } });
  }

  /**
   * Search coupons
   *
   * Search coupons using JSON body filters, search terms, includes, and sort instructions.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchCouponsResponseValue200ApplicationJson> {
    const { withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchCouponsResponseValue200ApplicationJsonResponse, SdkSearchCouponsRequestApplicationJsonResponse>(
      '/v1/coupons/search',
      serializeSdkSearchCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchCoupons', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkSearchCouponsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch create coupons
   *
   * Create multiple coupons in one request by sending a `resources` array.
   * @returns {Promise<SdkBatchCreateCouponsResponseValue200ApplicationJson>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async batchCreate(options: BatchCreateOptions): Promise<SdkBatchCreateCouponsResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkBatchCreateCouponsResponseValue200ApplicationJsonResponse, SdkBatchCreateCouponsRequestApplicationJsonResponse>(
      '/v1/coupons/batch',
      serializeSdkBatchCreateCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'batchCreateCoupons' },
    );
    return deserializeSdkBatchCreateCouponsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch update coupons
   *
   * Update multiple coupons in one request by sending a `resources` object keyed by coupon ID.
   * @param options - Additional query options.
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkBatchUpdateCouponsResponseValue200ApplicationJson>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async batchUpdate(options: BatchUpdateOptions): Promise<SdkBatchUpdateCouponsResponseValue200ApplicationJson> {
    const { withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkBatchUpdateCouponsResponseValue200ApplicationJsonResponse, SdkBatchUpdateCouponsRequestApplicationJsonResponse>(
      '/v1/coupons/batch',
      serializeSdkBatchUpdateCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'batchUpdateCoupons', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkBatchUpdateCouponsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch delete coupons
   *
   * Soft-delete multiple coupons by sending their IDs in `resources`.
   * @param options - Additional query options.
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<void>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async batchDelete(options: BatchDeleteOptions): Promise<void> {
    const { withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    await this.sellApp.deleteWithBody<SdkBatchDeleteCouponsRequestApplicationJsonResponse>('/v1/coupons/batch', serializeSdkBatchDeleteCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'batchDeleteCoupons', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } });
  }

  /**
   * List all coupons
   *
   * List your store's coupons, 15 per page by default. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2ListCoupons(options?: V2ListCouponsOptions): Promise<AutoPaginatable<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItemResponse, V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/coupons',
        deserializeV2ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
        options ? serializeV2ListCouponsOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<V2ListCouponsResponseValue200ApplicationJsonPropertyDataItemResponse, V2ListCouponsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/coupons',
          deserializeV2ListCouponsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      options ? serializeV2ListCouponsOptions(options) : undefined,
    );
  }

  /**
   * Create a coupon
   *
   * Create a discount code for your store. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkV2CreateCouponResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2CreateCoupon(options: V2CreateCouponOptions): Promise<SdkV2CreateCouponResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkV2CreateCouponResponseValue201ApplicationJsonResponse, SdkV2CreateCouponRequestApplicationJsonResponse>(
      '/v2/coupons',
      serializeSdkV2CreateCouponRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2CreateCoupon' },
    );
    return deserializeSdkV2CreateCouponResponseValue201ApplicationJson(data);
  }

  /**
   * Search coupons
   *
   * Search coupons using JSON body filters, search terms, includes, and sort instructions. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<V2SearchCouponsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2SearchCoupons(options: V2SearchCouponsOptions): Promise<SdkV2SearchCouponsResponseValue200ApplicationJson> {
    const { withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkV2SearchCouponsResponseValue200ApplicationJsonResponse, SdkV2SearchCouponsRequestApplicationJsonResponse>(
      '/v2/coupons/search',
      serializeSdkV2SearchCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2SearchCoupons', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkV2SearchCouponsResponseValue200ApplicationJson(data);
  }

  /**
   * Retrieve a coupon
   *
   * Retrieve a coupon by its ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkV2GetCouponResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2GetCoupon(options: V2GetCouponOptions): Promise<SdkV2GetCouponResponseValue200ApplicationJson> {
    const { coupon } = options;
    const { data } = await this.sellApp.get<SdkV2GetCouponResponseValue200ApplicationJsonResponse>(`/v2/coupons/${encodeURIComponent(coupon)}`, { ...options.requestOptions, operationId: 'v2GetCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } });
    return deserializeSdkV2GetCouponResponseValue200ApplicationJson(data);
  }

  /**
   * Update a coupon
   *
   * Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkV2ReplaceCouponResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2ReplaceCoupon(options: V2ReplaceCouponOptions): Promise<SdkV2ReplaceCouponResponseValue200ApplicationJson> {
    const { coupon, withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkV2ReplaceCouponResponseValue200ApplicationJsonResponse, SdkV2ReplaceCouponRequestApplicationJsonResponse>(
      `/v2/coupons/${encodeURIComponent(coupon)}`,
      serializeSdkV2ReplaceCouponRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2ReplaceCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkV2ReplaceCouponResponseValue200ApplicationJson(data);
  }

  /**
   * Update a coupon
   *
   * Change a coupon's discount, limits, or eligible products. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkV2UpdateCouponResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2UpdateCoupon(options: V2UpdateCouponOptions): Promise<SdkV2UpdateCouponResponseValue200ApplicationJson> {
    const { coupon, withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkV2UpdateCouponResponseValue200ApplicationJsonResponse, SdkV2UpdateCouponRequestApplicationJsonResponse>(
      `/v2/coupons/${encodeURIComponent(coupon)}`,
      serializeSdkV2UpdateCouponRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2UpdateCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkV2UpdateCouponResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a coupon
   *
   * Soft-delete a coupon. The returned coupon includes its deleted_at timestamp; use with_trashed or only_trashed to retrieve soft-deleted coupons later. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.coupon - The coupon path parameter.
   * @example 1
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2DeleteCoupon(options: V2DeleteCouponOptions): Promise<void> {
    const { coupon } = options;
    await this.sellApp.delete(`/v2/coupons/${encodeURIComponent(coupon)}`, { ...options.requestOptions, operationId: 'v2DeleteCoupon', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } });
  }

  /**
   * Batch create coupons
   *
   * Create multiple coupons in one request by sending a `resources` array. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkV2BatchCreateCouponsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2BatchCreateCoupons(options: V2BatchCreateCouponsOptions): Promise<SdkV2BatchCreateCouponsResponseValue200ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkV2BatchCreateCouponsResponseValue200ApplicationJsonResponse, SdkV2BatchCreateCouponsRequestApplicationJsonResponse>(
      '/v2/coupons/batch',
      serializeSdkV2BatchCreateCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2BatchCreateCoupons' },
    );
    return deserializeSdkV2BatchCreateCouponsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch update coupons
   *
   * Update multiple coupons in one request by sending a `resources` object keyed by coupon ID. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<SdkV2BatchUpdateCouponsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2BatchUpdateCoupons(options: V2BatchUpdateCouponsOptions): Promise<SdkV2BatchUpdateCouponsResponseValue200ApplicationJson> {
    const { withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkV2BatchUpdateCouponsResponseValue200ApplicationJsonResponse, SdkV2BatchUpdateCouponsRequestApplicationJsonResponse>(
      '/v2/coupons/batch',
      serializeSdkV2BatchUpdateCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2BatchUpdateCoupons', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } },
    );
    return deserializeSdkV2BatchUpdateCouponsResponseValue200ApplicationJson(data);
  }

  /**
   * Batch delete coupons
   *
   * Soft-delete multiple coupons by sending their IDs in `resources`. This v2 route is store-scoped. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.withTrashed - Include soft-deleted resources in the result.
   * @default false
   * @param options.onlyTrashed - Return only soft-deleted resources. Ignored when with_trashed is true.
   * @default false
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async v2BatchDeleteCoupons(options: V2BatchDeleteCouponsOptions): Promise<void> {
    const { withTrashed, onlyTrashed, requestOptions, ...payload } = options;
    await this.sellApp.deleteWithBody<SdkV2BatchDeleteCouponsRequestApplicationJsonResponse>('/v2/coupons/batch', serializeSdkV2BatchDeleteCouponsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'v2BatchDeleteCoupons', query: { ...(options.withTrashed !== undefined && { with_trashed: options.withTrashed }), ...(options.onlyTrashed !== undefined && { only_trashed: options.onlyTrashed }) } });
  }
}
