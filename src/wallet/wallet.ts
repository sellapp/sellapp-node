// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { WalletListOptions } from './interfaces/wallet-list-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { AdjustOptions } from './interfaces/adjust-options.interface';
import type { UpdateStatusOptions } from './interfaces/update-status-options.interface';
import type { ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem, ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItemResponse } from '../wallet-settings/interfaces/list-customer-wallets-response-value-200-application-json-property-data-item.interface';
import type { SdkGetCustomerWalletResponseValue200ApplicationJson, SdkGetCustomerWalletResponseValue200ApplicationJsonResponse } from '../wallet-settings/interfaces/sdk-get-customer-wallet-response-value-200-application-json.interface';
import type { SdkAdjustCustomerWalletResponseValue200ApplicationJson, SdkAdjustCustomerWalletResponseValue200ApplicationJsonResponse } from '../wallet-settings/interfaces/sdk-adjust-customer-wallet-response-value-200-application-json.interface';
import type { SdkUpdateCustomerWalletStatusResponseValue200ApplicationJson, SdkUpdateCustomerWalletStatusResponseValue200ApplicationJsonResponse } from '../wallet-settings/interfaces/sdk-update-customer-wallet-status-response-value-200-application-json.interface';
import type { SdkAdjustCustomerWalletRequestApplicationJsonResponse } from '../wallet-settings/interfaces/sdk-adjust-customer-wallet-request-application-json.interface';
import type { SdkUpdateCustomerWalletStatusRequestApplicationJsonResponse } from '../wallet-settings/interfaces/sdk-update-customer-wallet-status-request-application-json.interface';
import { deserializeListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem } from '../wallet-settings/serializers/list-customer-wallets-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkGetCustomerWalletResponseValue200ApplicationJson } from '../wallet-settings/serializers/sdk-get-customer-wallet-response-value-200-application-json.serializer';
import { deserializeSdkAdjustCustomerWalletResponseValue200ApplicationJson } from '../wallet-settings/serializers/sdk-adjust-customer-wallet-response-value-200-application-json.serializer';
import { deserializeSdkUpdateCustomerWalletStatusResponseValue200ApplicationJson } from '../wallet-settings/serializers/sdk-update-customer-wallet-status-response-value-200-application-json.serializer';
import { serializeSdkAdjustCustomerWalletRequestApplicationJson } from '../wallet-settings/serializers/sdk-adjust-customer-wallet-request-application-json.serializer';
import { serializeSdkUpdateCustomerWalletStatusRequestApplicationJson } from '../wallet-settings/serializers/sdk-update-customer-wallet-status-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class Wallet {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List customer wallets
   *
   * List customer wallet balances for the authenticated store. Requires the `wallet` or `wallet:write` API ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: WalletListOptions): Promise<AutoPaginatable<ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem, WalletListOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItemResponse, ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        '/v2/wallets',
        deserializeListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItemResponse, ListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          '/v2/wallets',
          deserializeListCustomerWalletsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Retrieve a customer wallet
   *
   * Retrieve one current-store customer wallet by customer ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customer - The customer path parameter.
   * @returns {Promise<SdkGetCustomerWalletResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetCustomerWalletResponseValue200ApplicationJson> {
    const { customer } = options;
    const { data } = await this.sellApp.get<SdkGetCustomerWalletResponseValue200ApplicationJsonResponse>(`/v2/wallets/${encodeURIComponent(customer)}`, { ...options.requestOptions, operationId: 'getCustomerWallet' });
    return deserializeSdkGetCustomerWalletResponseValue200ApplicationJson(data);
  }

  /**
   * Adjust a customer wallet
   *
   * Add or subtract a nonzero amount from a customer wallet. Requires the wallet:write API ability and store wallet permission. Use signed integer cents and a nonblank note. Positive adjustments can create a wallet and follow the store's credit-expiration policy; negative adjustments need an existing eligible wallet and sufficient balance. Wallets must be enabled. Send idempotency_key in the body. Replaying the same key returns the original entry only for the same store, customer, staff actor, amount and trimmed note; changed input returns 422. Keep that identity unchanged across retries. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customer - The customer path parameter.
   * @returns {Promise<SdkAdjustCustomerWalletResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async adjust(options: AdjustOptions): Promise<SdkAdjustCustomerWalletResponseValue200ApplicationJson> {
    const { customer, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkAdjustCustomerWalletResponseValue200ApplicationJsonResponse, SdkAdjustCustomerWalletRequestApplicationJsonResponse>(
      `/v2/wallets/${encodeURIComponent(customer)}/adjustments`,
      serializeSdkAdjustCustomerWalletRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'adjustCustomerWallet' },
    );
    return deserializeSdkAdjustCustomerWalletResponseValue200ApplicationJson(data);
  }

  /**
   * Update a customer wallet status
   *
   * Freeze or activate an existing customer wallet. Requires wallet:write and the store wallet permission, with wallets enabled. Activating a frozen wallet expires eligible credits before returning, so its balance may decrease. Read balance_cents from the returned wallet. Repeating the current status leaves the wallet unchanged. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customer - The customer path parameter.
   * @returns {Promise<SdkUpdateCustomerWalletStatusResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async updateStatus(options: UpdateStatusOptions): Promise<SdkUpdateCustomerWalletStatusResponseValue200ApplicationJson> {
    const { customer, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateCustomerWalletStatusResponseValue200ApplicationJsonResponse, SdkUpdateCustomerWalletStatusRequestApplicationJsonResponse>(
      `/v2/wallets/${encodeURIComponent(customer)}/status`,
      serializeSdkUpdateCustomerWalletStatusRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateCustomerWalletStatus' },
    );
    return deserializeSdkUpdateCustomerWalletStatusResponseValue200ApplicationJson(data);
  }
}
