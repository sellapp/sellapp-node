// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { BookingsCalendarEventsListOptions } from './interfaces/bookings-calendar-events-list-options.interface';
import type { SetOptions } from './interfaces/set-options.interface';
import type { SdkListBookingDateOverridesResponseValue200ApplicationJson, SdkListBookingDateOverridesResponseValue200ApplicationJsonResponse } from '../bookings/interfaces/sdk-list-booking-date-overrides-response-value-200-application-json.interface';
import type { SdkSetBookingDateAvailabilityResponseValue201ApplicationJson, SdkSetBookingDateAvailabilityResponseValue201ApplicationJsonResponse } from '../bookings/interfaces/sdk-set-booking-date-availability-response-value-201-application-json.interface';
import type { SdkSetBookingDateAvailabilityRequestApplicationJsonResponse } from '../bookings/interfaces/sdk-set-booking-date-availability-request-application-json.interface';
import { deserializeSdkListBookingDateOverridesResponseValue200ApplicationJson } from '../bookings/serializers/sdk-list-booking-date-overrides-response-value-200-application-json.serializer';
import { deserializeSdkSetBookingDateAvailabilityResponseValue201ApplicationJson } from '../bookings/serializers/sdk-set-booking-date-availability-response-value-201-application-json.serializer';
import { serializeSdkSetBookingDateAvailabilityRequestApplicationJson } from '../bookings/serializers/sdk-set-booking-date-availability-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

const serializeBookingsCalendarEventsListOptions = (options: BookingsCalendarEventsListOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.productVariantId !== undefined) wire.product_variant_id = options.productVariantId;
  if (options.from !== undefined) wire.from = options.from;
  if (options.to !== undefined) wire.to = options.to;
  if (options.status !== undefined) wire.status = options.status;
  if (options.pagination !== undefined) wire.pagination = options.pagination;
  if (options.page !== undefined) wire.page = options.page;
  return wire as PaginationOptions;
};

export class BookingsCalendarEvents {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List booking date overrides
   *
   * List store-wide or variant-specific booking date overrides. Date boundaries are stored in UTC and interpreted in the booking timezone. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SdkListBookingDateOverridesResponseValue200ApplicationJson>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: BookingsCalendarEventsListOptions): Promise<AutoPaginatable<SdkListBookingDateOverridesResponseValue200ApplicationJson, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<SdkListBookingDateOverridesResponseValue200ApplicationJsonResponse, SdkListBookingDateOverridesResponseValue200ApplicationJson>(
        this.sellApp,
        '/v2/booking-calendar-events',
        deserializeSdkListBookingDateOverridesResponseValue200ApplicationJson,
        options ? serializeBookingsCalendarEventsListOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<SdkListBookingDateOverridesResponseValue200ApplicationJsonResponse, SdkListBookingDateOverridesResponseValue200ApplicationJson>(
          this.sellApp,
          '/v2/booking-calendar-events',
          deserializeSdkListBookingDateOverridesResponseValue200ApplicationJson,
          params,
        ),
      options ? serializeBookingsCalendarEventsListOptions(options) : undefined,
    );
  }

  /**
   * Set booking date availability
   *
   * Atomically mark one or more local calendar dates unavailable or available for the store or one same-store booking variant. Requires the `listing` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkSetBookingDateAvailabilityResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async set(options: SetOptions): Promise<SdkSetBookingDateAvailabilityResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSetBookingDateAvailabilityResponseValue201ApplicationJsonResponse, SdkSetBookingDateAvailabilityRequestApplicationJsonResponse>(
      '/v2/booking-calendar-events',
      serializeSdkSetBookingDateAvailabilityRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'setBookingDateAvailability' },
    );
    return deserializeSdkSetBookingDateAvailabilityResponseValue201ApplicationJson(data);
  }
}
