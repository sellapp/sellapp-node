// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { AddOnsListOptions } from './interfaces/add-ons-list-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { SdkListAddOnsResponseValue200ApplicationJson, SdkListAddOnsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-list-add-ons-response-value-200-application-json.interface';
import type { SdkCreateAddOnDraftResponseValue201ApplicationJson, SdkCreateAddOnDraftResponseValue201ApplicationJsonResponse } from './interfaces/sdk-create-add-on-draft-response-value-201-application-json.interface';
import type { SdkSearchAddOnsResponseValue200ApplicationJson, SdkSearchAddOnsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-search-add-ons-response-value-200-application-json.interface';
import type { SdkGetAddOnResponseValue200ApplicationJson, SdkGetAddOnResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-add-on-response-value-200-application-json.interface';
import type { SdkReplaceAddOnResponseValue200ApplicationJson, SdkReplaceAddOnResponseValue200ApplicationJsonResponse } from './interfaces/sdk-replace-add-on-response-value-200-application-json.interface';
import type { SdkUpdateAddOnResponseValue200ApplicationJson, SdkUpdateAddOnResponseValue200ApplicationJsonResponse } from './interfaces/sdk-update-add-on-response-value-200-application-json.interface';
import type { SdkCreateAddOnDraftRequestApplicationJsonResponse } from './interfaces/sdk-create-add-on-draft-request-application-json.interface';
import type { SdkSearchAddOnsRequestApplicationJsonResponse } from './interfaces/sdk-search-add-ons-request-application-json.interface';
import type { SdkReplaceAddOnRequestApplicationJsonResponse } from './interfaces/sdk-replace-add-on-request-application-json.interface';
import { deserializeSdkListAddOnsResponseValue200ApplicationJson } from './serializers/sdk-list-add-ons-response-value-200-application-json.serializer';
import { deserializeSdkCreateAddOnDraftResponseValue201ApplicationJson } from './serializers/sdk-create-add-on-draft-response-value-201-application-json.serializer';
import { deserializeSdkSearchAddOnsResponseValue200ApplicationJson } from './serializers/sdk-search-add-ons-response-value-200-application-json.serializer';
import { deserializeSdkGetAddOnResponseValue200ApplicationJson } from './serializers/sdk-get-add-on-response-value-200-application-json.serializer';
import { deserializeSdkReplaceAddOnResponseValue200ApplicationJson } from './serializers/sdk-replace-add-on-response-value-200-application-json.serializer';
import { deserializeSdkUpdateAddOnResponseValue200ApplicationJson } from './serializers/sdk-update-add-on-response-value-200-application-json.serializer';
import { serializeSdkCreateAddOnDraftRequestApplicationJson } from './serializers/sdk-create-add-on-draft-request-application-json.serializer';
import { serializeSdkSearchAddOnsRequestApplicationJson } from './serializers/sdk-search-add-ons-request-application-json.serializer';
import { serializeSdkReplaceAddOnRequestApplicationJson } from './serializers/sdk-replace-add-on-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

const serializeAddOnsListOptions = (options: AddOnsListOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.page !== undefined) wire.page = options.page;
  if (options.pagination !== undefined) wire.pagination = options.pagination;
  if (options.withDrafts !== undefined) wire.with_drafts = options.withDrafts;
  if (options.onlyDrafts !== undefined) wire.only_drafts = options.onlyDrafts;
  return wire as PaginationOptions;
};

const serializeSearchOptions = (options: SearchOptions): PaginationOptions => {
  const wire: Record<string, unknown> = {
    maxItems: options.maxItems,
    maxPages: options.maxPages,
    requestOptions: options.requestOptions,
    limit: options.limit,
    before: options.before,
    after: options.after,
    order: options.order,
  };
  if (options.page !== undefined) wire.page = options.page;
  if (options.pagination !== undefined) wire.pagination = options.pagination;
  if (options.withDrafts !== undefined) wire.with_drafts = options.withDrafts;
  if (options.onlyDrafts !== undefined) wire.only_drafts = options.onlyDrafts;
  return wire as PaginationOptions;
};

export class AddOns {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List add-ons
   *
   * Retrieve the store's add-ons with compact variant references and ordered parent-product assignments. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SdkListAddOnsResponseValue200ApplicationJson>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: AddOnsListOptions): Promise<AutoPaginatable<SdkListAddOnsResponseValue200ApplicationJson, PaginationOptions>> {
    return new AutoPaginatable(
      await fetchAndDeserialize<SdkListAddOnsResponseValue200ApplicationJsonResponse, SdkListAddOnsResponseValue200ApplicationJson>(
        this.sellApp,
        '/v2/addons',
        deserializeSdkListAddOnsResponseValue200ApplicationJson,
        options ? serializeAddOnsListOptions(options) : undefined,
      ),
      (params) =>
        fetchAndDeserialize<SdkListAddOnsResponseValue200ApplicationJsonResponse, SdkListAddOnsResponseValue200ApplicationJson>(
          this.sellApp,
          '/v2/addons',
          deserializeSdkListAddOnsResponseValue200ApplicationJson,
          params,
        ),
      options ? serializeAddOnsListOptions(options) : undefined,
    );
  }

  /**
   * Create an add-on draft
   *
   * Create an add-on, optionally including its variant and parent_product_ids in one transaction. Set is_draft false with variant to publish immediately. Without variant, new add-ons remain drafts. Requires the listing API-key ability and store listing permission. Invalid variants or parent products roll back the complete creation. This call does not have response-replay idempotency; check for the created add-on before retrying a request whose response was lost. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkCreateAddOnDraftResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateAddOnDraftResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateAddOnDraftResponseValue201ApplicationJsonResponse, SdkCreateAddOnDraftRequestApplicationJsonResponse>(
      '/v2/addons',
      serializeSdkCreateAddOnDraftRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createAddOnDraft' },
    );
    return deserializeSdkCreateAddOnDraftResponseValue201ApplicationJson(data);
  }

  /**
   * Search add-ons
   *
   * Search add-ons by title, slug, or description and optionally filter or sort the result set. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SdkSearchAddOnsResponseValue200ApplicationJson>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchAddOnsResponseValue200ApplicationJson> {
    const { page, pagination, withDrafts, onlyDrafts, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchAddOnsResponseValue200ApplicationJsonResponse, SdkSearchAddOnsRequestApplicationJsonResponse>(
      '/v2/addons/search',
      serializeSdkSearchAddOnsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchAddOns', query: { ...(options.page !== undefined && { page: options.page }), ...(options.pagination !== undefined && { pagination: options.pagination }), ...(options.withDrafts !== undefined && { with_drafts: options.withDrafts }), ...(options.onlyDrafts !== undefined && { only_drafts: options.onlyDrafts }) } },
    );
    return deserializeSdkSearchAddOnsResponseValue200ApplicationJson(data);
  }

  /**
   * Retrieve an add-on
   *
   * Retrieve one add-on owned by the authenticated store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Additional query options.
   * @param options.addon - The addon path parameter.
   * @example 410
   * @param options.pagination - Set to false to return at most 100 matching items in one data array without pagination links or metadata.
   * @default true
   * @param options.withDrafts - Include draft add-ons alongside published add-ons.
   * @param options.onlyDrafts - Return only draft add-ons.
   * @returns {Promise<SdkGetAddOnResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetAddOnResponseValue200ApplicationJson> {
    const { addon } = options;
    const { data } = await this.sellApp.get<SdkGetAddOnResponseValue200ApplicationJsonResponse>(`/v2/addons/${encodeURIComponent(addon)}`, { ...options.requestOptions, operationId: 'getAddOn', query: { ...(options.pagination !== undefined && { pagination: options.pagination }), ...(options.withDrafts !== undefined && { with_drafts: options.withDrafts }), ...(options.onlyDrafts !== undefined && { only_drafts: options.onlyDrafts }) } });
    return deserializeSdkGetAddOnResponseValue200ApplicationJson(data);
  }

  /**
   * Update an add-on
   *
   * Update an add-on and optionally replace its ordered parent-product assignments. Publication is rejected unless the add-on has exactly one published, fixed-price, single-payment variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.addon - The addon path parameter.
   * @example 410
   * @returns {Promise<SdkReplaceAddOnResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceAddOnResponseValue200ApplicationJson> {
    const { addon, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceAddOnResponseValue200ApplicationJsonResponse, SdkReplaceAddOnRequestApplicationJsonResponse>(
      `/v2/addons/${encodeURIComponent(addon)}`,
      serializeSdkReplaceAddOnRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceAddOn' },
    );
    return deserializeSdkReplaceAddOnResponseValue200ApplicationJson(data);
  }

  /**
   * Update an add-on
   *
   * Update an add-on and optionally replace its ordered parent-product assignments. Publication is rejected unless the add-on has exactly one published, fixed-price, single-payment variant. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.addon - The addon path parameter.
   * @example 410
   * @returns {Promise<SdkUpdateAddOnResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateAddOnResponseValue200ApplicationJson> {
    const { addon, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateAddOnResponseValue200ApplicationJsonResponse, SdkReplaceAddOnRequestApplicationJsonResponse>(
      `/v2/addons/${encodeURIComponent(addon)}`,
      serializeSdkReplaceAddOnRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateAddOn' },
    );
    return deserializeSdkUpdateAddOnResponseValue200ApplicationJson(data);
  }

  /**
   * Delete an add-on
   *
   * Soft-delete an add-on and remove its product assignments. The deleted resource is returned for audit and reconciliation workflows. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.addon - The addon path parameter.
   * @example 410
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { addon } = options;
    await this.sellApp.delete(`/v2/addons/${encodeURIComponent(addon)}`, { ...options.requestOptions, operationId: 'deleteAddOn' });
  }
}
