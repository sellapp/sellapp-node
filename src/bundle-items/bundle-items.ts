// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { GetOptions } from './interfaces/get-options.interface';
import type { BundleItemsListOptions } from './interfaces/bundle-items-list-options.interface';
import type { AttachOptions } from './interfaces/attach-options.interface';
import type { DetachOptions } from './interfaces/detach-options.interface';
import type { SdkGetBundleItemResponseValue200ApplicationJson, SdkGetBundleItemResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-bundle-item-response-value-200-application-json.interface';
import type { ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem, ListBundleItemsResponseValue200ApplicationJsonPropertyDataItemResponse } from './interfaces/list-bundle-items-response-value-200-application-json-property-data-item.interface';
import type { SdkAttachBundleItemsResponseValue200ApplicationJson, SdkAttachBundleItemsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-attach-bundle-items-response-value-200-application-json.interface';
import type { SdkAttachBundleItemsRequestApplicationJsonResponse } from './interfaces/sdk-attach-bundle-items-request-application-json.interface';
import type { SdkDetachBundleItemsRequestApplicationJsonResponse } from './interfaces/sdk-detach-bundle-items-request-application-json.interface';
import { deserializeSdkGetBundleItemResponseValue200ApplicationJson } from './serializers/sdk-get-bundle-item-response-value-200-application-json.serializer';
import { deserializeListBundleItemsResponseValue200ApplicationJsonPropertyDataItem } from './serializers/list-bundle-items-response-value-200-application-json-property-data-item.serializer';
import { deserializeSdkAttachBundleItemsResponseValue200ApplicationJson } from './serializers/sdk-attach-bundle-items-response-value-200-application-json.serializer';
import { serializeSdkAttachBundleItemsRequestApplicationJson } from './serializers/sdk-attach-bundle-items-request-application-json.serializer';
import { serializeSdkDetachBundleItemsRequestApplicationJson } from './serializers/sdk-detach-bundle-items-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class BundleItems {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * Retrieve a bundle item
   *
   * Retrieve a specific item from a bundle by its product variant ID. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.bundle - The bundle path parameter.
   * @param options.item - The item path parameter.
   * @example 2
   * @returns {Promise<SdkGetBundleItemResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetBundleItemResponseValue200ApplicationJson> {
    const { bundle, item } = options;
    const { data } = await this.sellApp.get<SdkGetBundleItemResponseValue200ApplicationJsonResponse>(`/v2/bundles/${encodeURIComponent(bundle)}/items/${encodeURIComponent(item)}`, { ...options.requestOptions, operationId: 'getBundleItem' });
    return deserializeSdkGetBundleItemResponseValue200ApplicationJson(data);
  }

  /**
   * List bundle items
   *
   * Retrieve a paginated list of product variants attached to a bundle. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @param options.bundle - The bundle path parameter.
   * @returns {Promise<AutoPaginatable<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options: BundleItemsListOptions): Promise<AutoPaginatable<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem, Omit<BundleItemsListOptions, 'bundle'>>> {
    const { bundle, ...paginationOptions } = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItemResponse, ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>(
        this.sellApp,
        `/v2/bundles/${encodeURIComponent(bundle)}/items`,
        deserializeListBundleItemsResponseValue200ApplicationJsonPropertyDataItem,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<ListBundleItemsResponseValue200ApplicationJsonPropertyDataItemResponse, ListBundleItemsResponseValue200ApplicationJsonPropertyDataItem>(
          this.sellApp,
          `/v2/bundles/${encodeURIComponent(bundle)}/items`,
          deserializeListBundleItemsResponseValue200ApplicationJsonPropertyDataItem,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Attach bundle items
   *
   * Attach one or more product variants to a bundle with quantities. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.bundle - The bundle path parameter.
   * @returns {Promise<SdkAttachBundleItemsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async attach(options: AttachOptions): Promise<SdkAttachBundleItemsResponseValue200ApplicationJson> {
    const { bundle, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkAttachBundleItemsResponseValue200ApplicationJsonResponse, SdkAttachBundleItemsRequestApplicationJsonResponse>(
      `/v2/bundles/${encodeURIComponent(bundle)}/items/attach`,
      serializeSdkAttachBundleItemsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'attachBundleItems' },
    );
    return deserializeSdkAttachBundleItemsResponseValue200ApplicationJson(data);
  }

  /**
   * Detach bundle items
   *
   * Detach one or more product variants from a bundle. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.bundle - The bundle path parameter.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async detach(options: DetachOptions): Promise<void> {
    const { bundle, requestOptions, ...payload } = options;
    await this.sellApp.deleteWithBody<SdkDetachBundleItemsRequestApplicationJsonResponse>(`/v2/bundles/${encodeURIComponent(bundle)}/items/detach`, serializeSdkDetachBundleItemsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'detachBundleItems' });
  }
}
