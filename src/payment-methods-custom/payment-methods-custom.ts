// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import type { CreateOptions } from './interfaces/create-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { ReplaceOptions } from './interfaces/replace-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { DeleteOptions } from './interfaces/delete-options.interface';
import type { SdkListCustomPaymentMethodsResponseValue200ApplicationJson, SdkListCustomPaymentMethodsResponseValue200ApplicationJsonResponse } from '../payment-methods/interfaces/sdk-list-custom-payment-methods-response-value-200-application-json.interface';
import type { SdkCreateCustomPaymentMethodResponseValue201ApplicationJson, SdkCreateCustomPaymentMethodResponseValue201ApplicationJsonResponse } from '../payment-methods/interfaces/sdk-create-custom-payment-method-response-value-201-application-json.interface';
import type { SdkGetCustomPaymentMethodResponseValue200ApplicationJson, SdkGetCustomPaymentMethodResponseValue200ApplicationJsonResponse } from '../payment-methods/interfaces/sdk-get-custom-payment-method-response-value-200-application-json.interface';
import type { SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson, SdkReplaceCustomPaymentMethodResponseValue200ApplicationJsonResponse } from '../payment-methods/interfaces/sdk-replace-custom-payment-method-response-value-200-application-json.interface';
import type { SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson, SdkUpdateCustomPaymentMethodResponseValue200ApplicationJsonResponse } from '../payment-methods/interfaces/sdk-update-custom-payment-method-response-value-200-application-json.interface';
import type { SdkCreateCustomPaymentMethodRequestApplicationJsonResponse } from '../payment-methods/interfaces/sdk-create-custom-payment-method-request-application-json.interface';
import type { SdkReplaceCustomPaymentMethodRequestApplicationJsonResponse } from '../payment-methods/interfaces/sdk-replace-custom-payment-method-request-application-json.interface';
import type { SdkUpdateCustomPaymentMethodRequestApplicationJsonResponse } from '../payment-methods/interfaces/sdk-update-custom-payment-method-request-application-json.interface';
import { deserializeSdkListCustomPaymentMethodsResponseValue200ApplicationJson } from '../payment-methods/serializers/sdk-list-custom-payment-methods-response-value-200-application-json.serializer';
import { deserializeSdkCreateCustomPaymentMethodResponseValue201ApplicationJson } from '../payment-methods/serializers/sdk-create-custom-payment-method-response-value-201-application-json.serializer';
import { deserializeSdkGetCustomPaymentMethodResponseValue200ApplicationJson } from '../payment-methods/serializers/sdk-get-custom-payment-method-response-value-200-application-json.serializer';
import { deserializeSdkReplaceCustomPaymentMethodResponseValue200ApplicationJson } from '../payment-methods/serializers/sdk-replace-custom-payment-method-response-value-200-application-json.serializer';
import { deserializeSdkUpdateCustomPaymentMethodResponseValue200ApplicationJson } from '../payment-methods/serializers/sdk-update-custom-payment-method-response-value-200-application-json.serializer';
import { serializeSdkCreateCustomPaymentMethodRequestApplicationJson } from '../payment-methods/serializers/sdk-create-custom-payment-method-request-application-json.serializer';
import { serializeSdkReplaceCustomPaymentMethodRequestApplicationJson } from '../payment-methods/serializers/sdk-replace-custom-payment-method-request-application-json.serializer';
import { serializeSdkUpdateCustomPaymentMethodRequestApplicationJson } from '../payment-methods/serializers/sdk-update-custom-payment-method-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class PaymentMethodsCustom {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List custom payment methods
   *
   * List the current store's instruction and redirect payment methods. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkListCustomPaymentMethodsResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(requestOptions: RequestOptions = {}): Promise<SdkListCustomPaymentMethodsResponseValue200ApplicationJson> {
    const { data } = await this.sellApp.get<SdkListCustomPaymentMethodsResponseValue200ApplicationJsonResponse>('/v2/payment-methods/custom', requestOptions);
    return deserializeSdkListCustomPaymentMethodsResponseValue200ApplicationJson(data);
  }

  /**
   * Create a custom payment method
   *
   * Create a store-scoped custom payment method. Requires payment-method management permission. The effective redirect type needs a public redirect_url; instructions type needs nonblank instructions or a nonempty step. Switching to instructions clears redirect options; switching to redirect clears instructions, steps and proof-of-payment requirements. This API does not assign the method to every product. Inspect existing checkout configuration before changing an enabled method; creates have no idempotency guarantee. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @returns {Promise<SdkCreateCustomPaymentMethodResponseValue201ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async create(options: CreateOptions): Promise<SdkCreateCustomPaymentMethodResponseValue201ApplicationJson> {
    const { requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkCreateCustomPaymentMethodResponseValue201ApplicationJsonResponse, SdkCreateCustomPaymentMethodRequestApplicationJsonResponse>(
      '/v2/payment-methods/custom',
      serializeSdkCreateCustomPaymentMethodRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'createCustomPaymentMethod' },
    );
    return deserializeSdkCreateCustomPaymentMethodResponseValue201ApplicationJson(data);
  }

  /**
   * Retrieve a custom payment method
   *
   * Retrieve one custom payment method owned by the current store. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customPaymentMethod - Store-scoped custom payment method ULID.
   * @returns {Promise<SdkGetCustomPaymentMethodResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetCustomPaymentMethodResponseValue200ApplicationJson> {
    const { customPaymentMethod } = options;
    const { data } = await this.sellApp.get<SdkGetCustomPaymentMethodResponseValue200ApplicationJsonResponse>(`/v2/payment-methods/custom/${encodeURIComponent(customPaymentMethod)}`, { ...options.requestOptions, operationId: 'getCustomPaymentMethod' });
    return deserializeSdkGetCustomPaymentMethodResponseValue200ApplicationJson(data);
  }

  /**
   * Replace a custom payment method
   *
   * Update a custom payment method with required type and name. Omitted optional fields retain their stored values; this PUT does not reset every omitted field. Requires payment-method management permission. The effective redirect type needs a public redirect_url; instructions type needs nonblank instructions or a nonempty step. Switching to instructions clears redirect options; switching to redirect clears instructions, steps and proof-of-payment requirements. This API does not assign the method to every product. Inspect existing checkout configuration before changing an enabled method; creates have no idempotency guarantee. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customPaymentMethod - Store-scoped custom payment method ULID.
   * @returns {Promise<SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async replace(options: ReplaceOptions): Promise<SdkReplaceCustomPaymentMethodResponseValue200ApplicationJson> {
    const { customPaymentMethod, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.put<SdkReplaceCustomPaymentMethodResponseValue200ApplicationJsonResponse, SdkReplaceCustomPaymentMethodRequestApplicationJsonResponse>(
      `/v2/payment-methods/custom/${encodeURIComponent(customPaymentMethod)}`,
      serializeSdkReplaceCustomPaymentMethodRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'replaceCustomPaymentMethod' },
    );
    return deserializeSdkReplaceCustomPaymentMethodResponseValue200ApplicationJson(data);
  }

  /**
   * Update a custom payment method
   *
   * Partially update a store-scoped custom payment method; omitted fields retain their values. Requires payment-method management permission. The effective redirect type needs a public redirect_url; instructions type needs nonblank instructions or a nonempty step. Switching to instructions clears redirect options; switching to redirect clears instructions, steps and proof-of-payment requirements. This API does not assign the method to every product. Inspect existing checkout configuration before changing an enabled method; creates have no idempotency guarantee. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customPaymentMethod - Store-scoped custom payment method ULID.
   * @returns {Promise<SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateCustomPaymentMethodResponseValue200ApplicationJson> {
    const { customPaymentMethod, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateCustomPaymentMethodResponseValue200ApplicationJsonResponse, SdkUpdateCustomPaymentMethodRequestApplicationJsonResponse>(
      `/v2/payment-methods/custom/${encodeURIComponent(customPaymentMethod)}`,
      serializeSdkUpdateCustomPaymentMethodRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateCustomPaymentMethod' },
    );
    return deserializeSdkUpdateCustomPaymentMethodResponseValue200ApplicationJson(data);
  }

  /**
   * Delete a custom payment method
   *
   * Delete one store-scoped custom payment method and remove its product assignments. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.customPaymentMethod - Store-scoped custom payment method ULID.
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async delete(options: DeleteOptions): Promise<void> {
    const { customPaymentMethod } = options;
    await this.sellApp.delete(`/v2/payment-methods/custom/${encodeURIComponent(customPaymentMethod)}`, { ...options.requestOptions, operationId: 'deleteCustomPaymentMethod' });
  }
}
