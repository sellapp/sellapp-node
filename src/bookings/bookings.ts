// This file is auto-generated by oagen. Do not edit.

import type { SellApp } from '../sellapp';
import type { GetOptions as RequestOptions } from '../common/interfaces/get-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { BookingsListOptions } from './interfaces/bookings-list-options.interface';
import type { SearchOptions } from './interfaces/search-options.interface';
import type { GetOptions } from './interfaces/get-options.interface';
import type { UpdateOptions } from './interfaces/update-options.interface';
import type { CancelOptions } from './interfaces/cancel-options.interface';
import type { SdkListAppointmentsResponseValue200ApplicationJson, SdkListAppointmentsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-list-appointments-response-value-200-application-json.interface';
import type { SdkSearchAppointmentsResponseValue200ApplicationJson, SdkSearchAppointmentsResponseValue200ApplicationJsonResponse } from './interfaces/sdk-search-appointments-response-value-200-application-json.interface';
import type { SdkGetAppointmentResponseValue200ApplicationJson, SdkGetAppointmentResponseValue200ApplicationJsonResponse } from './interfaces/sdk-get-appointment-response-value-200-application-json.interface';
import type { SdkUpdateAppointmentResponseValue200ApplicationJson, SdkUpdateAppointmentResponseValue200ApplicationJsonResponse } from './interfaces/sdk-update-appointment-response-value-200-application-json.interface';
import type { SdkCancelAppointmentResponseValue200ApplicationJson, SdkCancelAppointmentResponseValue200ApplicationJsonResponse } from './interfaces/sdk-cancel-appointment-response-value-200-application-json.interface';
import type { SdkSearchAppointmentsRequestApplicationJsonResponse } from './interfaces/sdk-search-appointments-request-application-json.interface';
import type { SdkUpdateAppointmentRequestApplicationJsonResponse } from './interfaces/sdk-update-appointment-request-application-json.interface';
import type { SdkCancelAppointmentRequestApplicationJsonResponse } from './interfaces/sdk-cancel-appointment-request-application-json.interface';
import { deserializeSdkListAppointmentsResponseValue200ApplicationJson } from './serializers/sdk-list-appointments-response-value-200-application-json.serializer';
import { deserializeSdkSearchAppointmentsResponseValue200ApplicationJson } from './serializers/sdk-search-appointments-response-value-200-application-json.serializer';
import { deserializeSdkGetAppointmentResponseValue200ApplicationJson } from './serializers/sdk-get-appointment-response-value-200-application-json.serializer';
import { deserializeSdkUpdateAppointmentResponseValue200ApplicationJson } from './serializers/sdk-update-appointment-response-value-200-application-json.serializer';
import { deserializeSdkCancelAppointmentResponseValue200ApplicationJson } from './serializers/sdk-cancel-appointment-response-value-200-application-json.serializer';
import { serializeSdkSearchAppointmentsRequestApplicationJson } from './serializers/sdk-search-appointments-request-application-json.serializer';
import { serializeSdkUpdateAppointmentRequestApplicationJson } from './serializers/sdk-update-appointment-request-application-json.serializer';
import { serializeSdkCancelAppointmentRequestApplicationJson } from './serializers/sdk-cancel-appointment-request-application-json.serializer';

interface SellAppResponseEnvelope<T> { data: T; status: number; headers: Headers; requestId?: string }
type SellAppWithResponse<T extends object> = {
  [K in keyof T as T[K] extends (...args: any[]) => Promise<any> ? K : never]:
    T[K] extends (...args: infer A) => Promise<infer R> ? (...args: A) => Promise<SellAppResponseEnvelope<R>> : never;
};
function createResourceWithResponse<T extends object>(resource: T, client: SellApp): SellAppWithResponse<T> {
  return new Proxy({} as SellAppWithResponse<T>, {
    get: (_target, property) => (...args: unknown[]) => client.captureResponse(
      () => (resource[property as keyof T] as (...values: unknown[]) => Promise<unknown>).apply(resource, args),
    ),
  }) as SellAppWithResponse<T>;
}

export class Bookings {
  constructor(private readonly sellApp: SellApp) {}
  get withResponse(): SellAppWithResponse<this> { return createResourceWithResponse(this, this.sellApp); }

  /**
   * List appointments
   *
   * List appointments for the selected store. Requires the `invoice` Sanctum ability and invoice permission. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SdkListAppointmentsResponseValue200ApplicationJson>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async list(options?: BookingsListOptions): Promise<AutoPaginatable<SdkListAppointmentsResponseValue200ApplicationJson, BookingsListOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<SdkListAppointmentsResponseValue200ApplicationJsonResponse, SdkListAppointmentsResponseValue200ApplicationJson>(
        this.sellApp,
        '/v2/bookings',
        deserializeSdkListAppointmentsResponseValue200ApplicationJson,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<SdkListAppointmentsResponseValue200ApplicationJsonResponse, SdkListAppointmentsResponseValue200ApplicationJson>(
          this.sellApp,
          '/v2/bookings',
          deserializeSdkListAppointmentsResponseValue200ApplicationJson,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Search appointments
   *
   * Search appointment UUIDs or customer email addresses and compose store-scoped filters and sorting. The public `id` filter targets the UUID returned as `id`; `uuid` remains an equivalent compatibility filter. Requires the `invoice` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<SdkSearchAppointmentsResponseValue200ApplicationJson>>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async search(options: SearchOptions): Promise<SdkSearchAppointmentsResponseValue200ApplicationJson> {
    const { page, pagination, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.post<SdkSearchAppointmentsResponseValue200ApplicationJsonResponse, SdkSearchAppointmentsRequestApplicationJsonResponse>(
      '/v2/bookings/search',
      serializeSdkSearchAppointmentsRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'searchAppointments', query: { ...(options.page !== undefined && { page: options.page }), ...(options.pagination !== undefined && { pagination: options.pagination }) } },
    );
    return deserializeSdkSearchAppointmentsResponseValue200ApplicationJson(data);
  }

  /**
   * Retrieve an appointment
   *
   * Retrieve a store-scoped appointment by UUID or numeric ID. Provider error details are intentionally not exposed. Requires the `invoice` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.booking - The appointment UUID or numeric ID.
   * @example "018f61d6-1c46-7b42-8a94-522bc6b5c53f"
   * @returns {Promise<SdkGetAppointmentResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async get(options: GetOptions): Promise<SdkGetAppointmentResponseValue200ApplicationJson> {
    const { booking } = options;
    const { data } = await this.sellApp.get<SdkGetAppointmentResponseValue200ApplicationJsonResponse>(`/v2/bookings/${encodeURIComponent(booking)}`, { ...options.requestOptions, operationId: 'getAppointment' });
    return deserializeSdkGetAppointmentResponseValue200ApplicationJson(data);
  }

  /**
   * Update an appointment
   *
   * Reschedule an appointment atomically. Current availability is verified under the same conflict lock used by checkout holds. Requires the `invoice` Sanctum ability. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.booking - The appointment UUID or numeric ID.
   * @example "018f61d6-1c46-7b42-8a94-522bc6b5c53f"
   * @returns {Promise<SdkUpdateAppointmentResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async update(options: UpdateOptions): Promise<SdkUpdateAppointmentResponseValue200ApplicationJson> {
    const { booking, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkUpdateAppointmentResponseValue200ApplicationJsonResponse, SdkUpdateAppointmentRequestApplicationJsonResponse>(
      `/v2/bookings/${encodeURIComponent(booking)}`,
      serializeSdkUpdateAppointmentRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'updateAppointment' },
    );
    return deserializeSdkUpdateAppointmentResponseValue200ApplicationJson(data);
  }

  /**
   * Cancel an appointment
   *
   * Cancel a store-owned appointment. Cancellation is the only supported administrative status transition. OAuth callers use the admin grant and select a store with X-STORE. Current membership and role permissions apply to every request.
   * @param options - The request options.
   * @param options.booking - The appointment UUID or numeric ID.
   * @example "018f61d6-1c46-7b42-8a94-522bc6b5c53f"
   * @returns {Promise<SdkCancelAppointmentResponseValue200ApplicationJson>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   * @throws {RateLimitExceededException} 429
   */
  async cancel(options: CancelOptions): Promise<SdkCancelAppointmentResponseValue200ApplicationJson> {
    const { booking, requestOptions, ...payload } = options;
    const { data } = await this.sellApp.patch<SdkCancelAppointmentResponseValue200ApplicationJsonResponse, SdkCancelAppointmentRequestApplicationJsonResponse>(
      `/v2/bookings/${encodeURIComponent(booking)}/status`,
      serializeSdkCancelAppointmentRequestApplicationJson(payload), { ...options.requestOptions, operationId: 'cancelAppointment' },
    );
    return deserializeSdkCancelAppointmentResponseValue200ApplicationJson(data);
  }
}
